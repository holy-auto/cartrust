# NOTE_CANDIDATES.md — note記事候補

> note等の外部発信で書けそうなネタをストックする場所。思いついたら都度追記し、
> 実際に書いたら状態を「執筆済み」にして公開先リンクを追記する。

## 「送信しました」と出る画面が、お客様には届いていなかった話
- 切り口: 会計後にレシートをお客様へ送る機能を点検したら、**送ったリンクが必ずエラー画面に
  なっていた**。店側の画面には「送信しました」と出る。SMS アプリを開くところまでは成功するので、
  **壊れているのは受け取った側だけ**。誰も報告しないまま動き続けていた。
  原因は、URL を組み立てる1行が**別の書類の公開ページ**を指していたこと。
  さらに私は、その壊れた1行を新しい画面にコピーして**壊れ方を2箇所に増やしていた**。
  変数の名前が「レシートのURL」で、値が自社のドメインで始まっていたので、
  中身を確かめずに「動いているものを再利用した」つもりになっていた。
- 学び: **コピーは「動いている証拠」ではない。** 既存コードを持ってくるとき、
  「これは動いているはずだ」の根拠は「そこに書かれていたから」でしかないことがある。
  特に**送信先が外にあるもの**（メール・SMS・Webhook・QR）は、送る側の画面が
  成功と出ても受け取る側が壊れていることがある。**成功の判定が送信側にしか無い機能は、
  受け取り側を一度は自分で開く。**
  対策として、URL の組み立てを1箇所に集め、**行き先を自動テストで固定した**
  （書き換えるとテストが落ちる）。文字列を画面に直接書かない限り、次は同じ形にならない。
- 公開区分: **公開可**（ドメイン名・ルート名・テナント名・トークンの実値は書かない）

## 新しいものを置く前に、隣に何があるか見る
- 切り口: 新しい公開ページを作るとき、置き場所に**既に別のものが入っていた**。
  仕組み上そこには1種類しか置けないので、そのままだとビルドが通らない。
  同じ日に同じ形のミスをもう1回した ―― 部品を1箇所にまとめようとして新しいファイルを作ったが、
  **まとめ先のファイルは既に存在していた**。どちらも一覧を1回見れば分かる話だった。
- 学び: 型チェックもテストも全部緑だった。**どちらの誤りも型に現れない。**
  片方はビルドでしか出ず、もう片方はそもそも壊れていない（ただの重複なので誰も落ちない）。
  「緑だから大丈夫」が2回とも通ってしまう。
  習慣にしたのは2つ。**新しいファイルを作る前に、置くディレクトリの中身を見る。**
  **公開前にビルドを1回通す**（型チェックとテストでは出ない種類の壊れ方があるため）。
- 公開区分: **公開可**

## 「オフにする」設定が存在しないとき — ライブラリの想定外を、設定で殴らずに回避する

- 切り口: アプリの起動画面を「ロゴなしの単色」にしたかったが、使っているフレームワークには
  その設定が無かった。画像を指定しなければロゴは出ない、と思って消したら、
  画面の定義ファイルにはロゴへの参照だけが残り、参照先が無いのでビルドが落ちた。
  ソースを読むと「参照を書く処理」と「実体を書く処理」で条件分岐が食い違っており、
  さらに「昔の挙動に戻す」分岐は `null` と `undefined` の取り違えで**到達不能**になっていた。
  解決策は「背景と同じ色の画像を渡す」。オフにできないなら、
  オンのまま見えなくすればいい。フレームワークにパッチを当てるでも、
  依存を増やすでもなく、1902バイトの単色画像1枚で済んだ。
- 想定読者: OSS のフレームワーク上でプロダクトを作っている開発者
- 状態: 未着手
- メモ: 整備でも同じ判断をする。「そのスイッチは存在しない」と分かったとき、
  配線を切って無理やり止めるのか、動いたまま影響が出ない状態にするのか。
  後者の方が壊れにくいことが多い。
  材料: 該当コードの条件分岐（参照側は無条件、実体側は `if (image)` で保護）、
  `null !== undefined` で死んでいる分岐、透明ではなく同色にした理由
  （空アイコンをアプリアイコンで置き換える端末実装がありうる）。
  検証として「事故そのものを再現して検査が落ちること」を確認した手順も書ける。
- 公開区分: 公開可

## CIが全部緑なのに本番ビルドが落ちる — 「静的に分かることは静的に止める」

- 切り口: モバイルアプリの Android ビルドが「一度も通っていなかった」ことに、
  実機確認を始めて初めて気づいた話。決済端末ライブラリが Android 8.0 以上を要求しているのに、
  アプリ側の設定が 7.0 のままだった。この矛盾は設定ファイルを2つ見比べれば数秒で分かるのに、
  17分かけてビルドが失敗するまで誰も気づかなかった。CI は型チェックとテストだけを回していて、
  ネイティブの設定は一切見ていなかったため。フルビルドを CI に載せる（1回20分）のではなく、
  設定の整合だけを数秒で検査する仕組みを足して決着させた判断。
- 想定読者: モバイルアプリを内製している開発者 / 小規模チームで CI を設計する人
- 状態: 未着手
- メモ: 整備の世界にも同じ構造がある。「エンジンをかけてみないと分からない不具合」と
  「点検表を見比べれば分かる不具合」は別物で、後者を試運転で見つけるのは時間の使い方として間違っている。
  数字: ビルド失敗 2回（16分58秒 / 11分47秒）に対して、追加した検査は GitHub Actions 実測で
  prebuild 3秒＋検査 1秒未満（CI ジョブ全体で 39秒）。桁が3つ違う。
  検査を入れる前の状態を再現して、実際に落ちることを確認してから採用した点も材料になる。
- 公開区分: 公開可

## 「保存して速くする」が正解とは限らない — 演出の尺が設計判断を変えた話
- 切り口: アプリ起動時のちらつきを「値をディスクに保存すれば直る」と考えたが、
  調べると**保存で稼げる時間の使い道が無い**ことが分かった、という話。
  直前に入れた起動アニメーションが1.85秒の下限を持っていたため、保存で短縮できる
  ネットワーク1往復はその尺に完全に隠れる。利得ゼロ。
  一方で保存の代償は具体的に5つ数えられた。ログアウト経路の1つが店舗選択を消さないので
  セッションより長生きする、無効化された店舗に固定されるとエラーも出ずに全タブが空になる、
  正規化なしでINSERTしている3箇所がFK違反を起こす、など。
  結論は「保存して速くする」ではなく「毎回取り直すが、見えない時間に隠す」。
  往復の総数は同じで、見える位置だけが変わる。
  一般化すると「最適化の利得は、それが露出する場所があって初めて価値になる」。
  同じ最適化でも、手前に1.85秒の演出があるかないかで正解が入れ替わる。
- 想定読者: 個人開発者 / パフォーマンス改善をやる人 / SaaS のPM
- 状態: 未着手
- メモ: 「よく知られた正解（キャッシュする・永続化する）を、自分の状況で採点し直す」が主題。
  代償を5つ数えて並べたら判断が一瞬でついた、という点も書ける。
  変異テストで「2件以上でもデフォルト店舗を選んでしまう」バグを検出できることを
  確認した話も繋げられる（親切心で壊れやすい箇所を固定する）。

## 「かっこいいから」で終わらせない起動アニメーション — 目で見て決めず、測って決めた3つのこと
- 切り口: アプリの起動演出という、いかにも感覚で決めそうな仕事を、全部実測で詰めた話。
  (1) ブランドロゴを暗い紺地に置こうとして、コントラスト比を測ったら **1.14:1** だった。
  目視では「まあ見える」と思っていたが、数字を見た瞬間に案が消えた。ロゴは白背景前提の
  デザインだった、という事実が測って初めて分かった。
  (2) 「光るモーションが途中で終わって気持ち悪い」という感想を、1フレーム刻みで高輝度画素を
  数えて原因を特定した。ハイライトのスイープが2.633秒に始まって完結しないまま素材が
  3.000秒で切れていた。感想は正しく、原因は感想の言葉の中には無かった。
  (3) スプラッシュ画像に、ロゴが現れ始めた極小の芽が写っていた。10260画素中15画素。
  目では気づきにくいが、ネイティブスプラッシュは起動中ずっと出たままなので、
  この点が数百ms固まって見えると「描画バグ」に見える。
  加えて、依頼を受けて素材を確認したら**アプリアイコンがExpoのデフォルトのままだった**という
  「頼まれていないが直さないと依頼が成立しない」問題の扱いも書ける。
- 想定読者: 個人開発者 / モバイルアプリのUIを担当する人 / AIに実装を任せている人
- 状態: 未着手
- メモ: 主題は「感覚の仕事こそ、判断の分かれ目を数字にする」。
  ffmpeg で画素を数えるだけで、デザインの議論が意見の応酬から事実確認に変わる。
  変異テスト（わざと壊してチェックが落ちるか確かめる）で3種のバグを検出できることを
  確認した話も、同じ「確かめる」の系列として繋げられる。

## 「CIは全部緑」でも、本番と設計図は 68 個ずれていた

- 何があったか: 「使われていない権限テーブルが本番にだけ在り、マイグレーション（設計図）に
  定義が無い」という事故の後始末で、**同じことが他にも起きていないか**を機械的に数えた。
  結果、テーブル23・ビュー1・関数24・トリガ15・型5 の**計 68 個**が、
  設計図に無いまま本番で動いていた。
- なぜ検査をすり抜けたか: 検査は2つあった。どちらも**この形のずれを見ない作り**だった。
  - 設計図の再生検査 — 空のデータベースに設計図を全部流して「エラーが出ないか」を見る。
    **できあがったものを本番と比べていない**ので、本番にだけ在る物は原理的に出ない。
  - スキーマ照合検査 — 照合の基準を**本番からコピーして**作っている。
    ずれごと写して合格する。
  つまり「緑」は「設計図に矛盾が無い」であって、「本番が設計図どおり」ではなかった。
- 危険度は2つに割れた: **テーブル23個は無害**だった（21個が0行、アプリからの参照ゼロ、
  全件アクセス制御が有効）。危なかったのは**関数**で、設計図に無い関数5本を
  **35本のアクセス制御ルール**が呼んでいた。**設計図だけから作り直したデータベースは、
  本番と同じ権限判定をしない。** 復旧のときに初めて分かる種類の欠落である。
- 何が言えるか: 検査の緑は「何を比べたか」以上のことを保証しない。
  **「本番と設計図が一致しているか」を比べる検査は、意外とどこにも無い。**
  作った当人は「マイグレーションで管理している」と思っているし、
  ダッシュボードで1回手を入れたことは半年後には誰も覚えていない。
- 公開区分: 公開可（テーブル名・件数のみ。データの中身・個人情報は含まない）

## テストは緑、機能は壊れていた — 「その語が書いてある」を「そう動く」と読んだ

- 何があったか: 「AIが作った文面を、人が公開前に目視確認する」仕組みを作った。確認が今も有効かを
  表す印を作り、生成側と公開側で突き合わせる。印に版の情報が入っていることをテストで固定し、
  **壊すと落ちることも確かめた**。緑だった。自動レビューが指摘した ——「その状態では、
  公開が1件も通りません」。
- 何が起きていたか: 印は片側が**プログラムが作った時刻の文字列**、もう片側が**データベースが
  返した時刻の文字列**から作られていた。同じ瞬間なのに表記が違う（末尾が `Z` か `+00:00` か）。
  ハッシュは永久に一致しない。**確認しても必ず「内容が最新ではありません」で弾かれる。**
- なぜ緑だったか: テストが確かめていたのは「その関数の中に、版を表す語が**書いてある**」ことだけ。
  **生成側と公開側が同じ値を渡すかは、一度も走らせていなかった。** ソースを検索する形の検査は、
  文字列の有無しか語れない。片方が `Z`、片方が `+00:00` でも緑のままである。
- 効かなかった対策: 「検査を壊して落ちることを確かめる」は実施済みだった。だがそれで分かるのは
  **文字列の変化を検出できる**ことだけで、**その文字列が正しい動作を意味するか**は出てこない。
- 直し方: 表記を揃えるのではなく、**両側の出所を揃えた**。片方が「送った値」、もう片方が
  「返ってきた値」になっている構造そのものが原因で、同じ罠は時刻以外の項目にも残っていた。
- 結論: 「書いてある」と「そう動く」の間には距離がある。**構造を見る検査を書いたら、
  「これが緑のまま壊せる形」を1つ挙げてみる。** 挙げられたら、その形を潰す検査をもう1本足す。
  AI に開発させる文脈で特に効く —— AI は緑を完了の合図として扱いやすい。
- 併せて: 自動レビューは4巡し、うち1巡は**私が直した結果できた穴**を突いた。直したことより、
  直した後にもう一度見てもらうことに価値があった。
- 公開区分: **公開可**（技術的な一般論のみ。店名・顧客情報・自社の権限構成・
  取引先には触れない。使用したレビューツール名は伏せて「自動レビュー」と書く）
- 状態: 未執筆

## 「日付を確かめる」だけの自動チェックが、書き込み権限の受け渡し口になっていた
- 切り口: レビュー待ちが長くなった変更を毎日見回って、日付のズレを知らせるだけの
  自動チェックを作った。やることは日付の比較ひとつ。危ないことは何もしていない、
  つもりだった。
- 実際にやっていたこと: そのチェックは**相手が書いたプログラムを、こちらの権限で
  実行していた**。相手のファイルを持ってきて、そのまま動かす作りだったため。
  日付を見るだけのはずが、「うちの鍵を渡して好きに動かしてもらう」形になっていた。
- もう一つの副作用: 動かしていたのが**相手側のチェック**だったので、
  相手が「問題なし」と言えばそれが通る。**見張りたかった相手に、
  自分が問題ないかを自己申告させていた。**
- なぜ気づけなかったか: **「何を実行するか」は考えたのに、「誰が書き換えられるものを
  実行するか」を考えなかった。** 手元で同じ操作をすると自分のファイルが出てくるので、
  差が見えない。自動化した瞬間に、同じ操作が別の意味になる。
- 直し方: 相手のプログラムは持ってこない。**必要なのはデータ（日付）だけ**なので、
  データだけを取り出して、こちら側のプログラムで読む。
- 学び: 自動化を1つ足すときは、**「この処理の権限で、誰の書いたものが動くか」を
  一文で言えるか**を確かめる。答えに自分以外が出てきたら設計を変える。
  「安全な処理だから」は理由にならない。危ないのは処理の内容ではなく、
  権限と実行するコードの持ち主の組み合わせ。
- 状態: 未執筆
- 公開区分: 公開可（一般的な自動化設計の教訓。具体的な設定値・トークン・内部構成は書かない）

## コピペで散った5つの処理を1つにまとめたら、まとめた先で二重に記録していた

- 切り口: 同じ処理が5箇所にコピペで散っていて、しかも**中身がすでに食い違っていた**
  （時刻を記録しない経路、履歴に残らない経路があった）。1本にまとめて、全経路で
  記録が揃うようにした。ここまでは狙いどおり。
- 実際に起きたこと: **まとめた共通処理も履歴を1行書く**。呼び出し側の1つは、
  自分でも履歴を書いていた。共通化する前は「片方だけが書いていた」ので正しかったのに、
  共通化した瞬間に**同じ出来事が2行になった**。自動レビューが指摘するまで気づかなかった。
- なぜ気づけなかったか: 「散らばった処理をまとめる」ときに、**まとめる側の中身は読んだが、
  呼び出し側に残る処理を1行ずつ読まなかった**。「共通化したから呼び出し側は薄くなる」と
  思い込んで、実際に何が残っているかを見なかった。
- もう一つ: まとめるときに**処理の順番を1つ入れ替えていた**。結果、すでに無効な対象を
  もう一度無効にすると「成功」と返り、**実際には何も起きていないのに記録だけが残る**
  状態になっていた。まとめる作業は、順番も一緒に持ち込む。
- 学び: **共通化は「足す」作業ではなく「引く」作業。** 共通処理に何を入れたかではなく、
  **呼び出し側から何が消えるはずか**を先に書き出す。消えるはずのものが残っていたら、
  それが二重になる。そして「まとめただけ、挙動は変えていない」と言う前に、
  **順番が同じかを確かめる**。
- 状態: 未執筆
- 公開区分: 公開可（設計・リファクタリングの一般的な教訓。テーブル名・内部構成は書かない）

## 安全ブレーキが3か月効いていなかった。壊れていたのは装置ではなく「既定値」だった
- 切り口: AI の暴走課金を止める月次上限を実装してあった。コードもテストもある。
  **設定が無いときに「上限なし」を返す**ようになっていて、
  本番はその「設定が無いとき」だった。装置は正しく動いていて、
  ただ一度も作動条件に入らなかった。
- なぜ気づけなかったか: 「上限を超えたら止まるか」は検証したが、
  **「上限そのものがどこから来るか」を検証していなかった**。
  条件式は読んだのに、その値の出どころを追わなかった。
- 直したあと、同じ穴をもう一度開けかけた: 「明示的に 0 を入れたら上限なし」という
  逃げ道を用意した。ところが**設定ファイルのサンプルが長らく 0 を配っていた**ので、
  その 0 は「意思表示」ではなく「ただの初期値」。逃げ道を尊重すると、
  **まさに守りたい環境でブレーキが外れたまま**になる。逃げ道は撤回した。
- 学び: **安全装置に「切る」設定を、既定値と同じ表現で持たせてはいけない。**
  「未設定」と「明示的に切った」が区別できなくなる。
  そして安全装置の既定は、**設定漏れで外れる側ではなく、効く側に倒す**。
- 状態: 未執筆
- 公開区分: 公開可（設計上の一般的な教訓。金額・テナント情報は書かない）

## 「4件ある」と書いた。grep が数えていたのは別のものだった
- 切り口: ある不具合と同じ形が他に無いか grep で探し、4件ヒットしたので
  「同じ不具合が4画面ある」と報告書に書いた。1件は違った。
  **grep が引いたのは「変換のコード」で、私が主張したのは「入力欄の種類」**。
  同じコードでも、入力欄の種類が違えば意味が変わる。語彙がずれたまま件数を書いた。
- 学び: **検索結果の件数を主張の件数として書くときは、
  「検索した言葉」と「主張する言葉」が同じかを声に出して確かめる。**
  ずれているなら、ずれている分を1件ずつ埋めるまで件数を書いてはいけない。
  埋めないなら「この形が N 箇所ある（中身は未確認）」と正直に書く。
- 状態: 未執筆
- 公開区分: 公開可（調査手法の一般的な教訓）
## 提出書類は「実物と一致」していないと逆効果 — 自動レビューが文書の齟齬を一件ずつ暴いた

- 何があったか: あるコンプライアンス申請のために、実装の説明文書一式を書いた。自動コードレビューが
  push のたびに「その記述、実物と違う」を1件ずつ返してきた ——「このスキャナは実は自動実行されていない」
  「この認証は既定でオフ」「この保護は書いてある仕組みではなく別の仕組み」等。確かめると全部その通りだった。
- なぜ起きたか: ツールが存在することは事実でも、**『いま稼働しているか』を確かめずに“現行の管理策”として書いた**。
  審査に出す文書は「あるべき姿」ではなく「今そうなっている姿」を書くもの。名前を並べるだけでは足りない。
- もう一段: 成果物（生成物そのもの）も過大主張していた。「実写を撮影した」「メタデータを除去した」を、
  実際には撮影経路が保証されていない・除去対象が無い場合にも主張していた。しかも **入口を1つ塞いで満足し、
  もう1つ見落とした**（同種の見落としを同じ作業内で繰り返した）。
- 結論: 提出書類も生成物も、**一次ソース（実際のコード・設定・入力経路）と1件ずつ突き合わせる**まで完成ではない。
  「もっともらしく正しく読める」ことは、正しいことの証拠にならない。緑や流暢さは「その範囲では問題ない」以上を語らない。
- 公開区分: **要確認**（教訓は一般論として公開可だが、申請中の具体の制度名・自社のセキュリティ構成・申請ステータスに
  触れるため公開範囲は代表が確認。制度名や自社構成の詳細は伏せ、教訓のみ一般化して書く）
- 状態: 未執筆

## 検査が全部緑でも、検査が見ていない場所は壊れる — 本番だけが持っているもの

- 何があったか: データベースの変更履歴を「まっさらな状態から流し直せるか」検査する仕組みを
  直していた。通らなかった 203 本を 0 本にして、CI も本番プレビューも全部緑になった。
  そこで自動レビューが1件だけ指摘した ——「その足したファイル、**本番より過去の日付**ですよ」。
- 何が起きるところだったか: 本番のデータベースには「どこまで適用したか」の台帳がある。
  台帳の最新より古いものを後から足すと、適用ツールは順序が壊れたと判断して**止まる**。
  止まると、それ以降の変更が本番へ一切届かなくなる。同じ形で過去に13日間止まり、
  その間ずっと気づかれていなかった。
- なぜ緑だったか: 検査はどれも「**まっさらな状態**に流せるか」しか見ていない。
  本番はまっさらではない。**検査の対象と、守りたいものが違っていた。**
- しかも社内文書には、この不変条件が20行書いてあった。数日前に自分が書いた
  「CI の緑を、本番との突き合わせの代わりにしない」という一文も残っていた。読まずにやった。
- 結論: 緑は「**その検査が見ている範囲では問題ない**」以上のことを言わない。
  緑を見たときに毎回問うべきなのは「で、これは何を見ていないのか」。
  AI に開発させる文脈では特に効く。AI は緑を完了の合図として扱いやすい。
- 公開区分: 公開可（日付連番のバージョン番号のみで、取引先名・顧客名・機密を含まない）
- 状態: 未執筆

## 「直せない」と書いてあったので、3日間そのままにした — 測ったら半日で直った

- 何があったか: CI の1つが最古のマイグレーションで落ち続けていた。リポジトリの運用文書に
  「履歴を書き換えない限り直せない」「恒久対応は履歴を畳み直すこと」と書いてあり、
  それを引用して「この変更のせいではない」と3回コメントした。説明は正しかった。
  正しかったから、そこで止まった。
- 転機: 「ブランチ切って対応しよう」と言われて初めて**測った**。438 本中 203 本が
  落ちていて、うち 170 本は**先頭3本の連鎖**だった。その3本を直すだけで 33 本に減った。
- 結論: **説明がつくことと、直せないことは別。** そして「難しい」と書いた人が使った
  道具（ここでは多重パスの検査）が、今の問いに答える形になっているとは限らない。
  古い検査は順序の逆転を吸収してしまうので、**CI は緑なのにプレビューだけ赤**という、
  いちばん気づきにくい形を作っていた。
- AI に開発させる文脈での含み: AI は「既知の問題」と書かれたものを疑わない。
  文書は事実ではなく、**過去の誰かの判断**。その判断の根拠を1回走らせるだけで
  ひっくり返ることがある。
- 公開区分: 公開可（具体的な取引先名・顧客名を含まない。数字は自分の環境で測ったもの）

## 職人の実績は、誰のものか — 外注の記録を残そうとして出てきた設計の話
- 切り口: 外注に出した作業の記録を「誰の名前で残すか」を決めようとしたら、
  技術の問題ではなく事業の問題だと分かった。元請けの名義にすればお客さんの体験は自然になる。
  外注先の名義にすれば履歴の真正性は保てるが、お客さんは知らない会社名が入った証明書を受け取る。
- 掘る点その1: 元請け名義を選んだ。理由は、記録を見るのはお客さんで、お客さんにとっての
  取引相手は元請けだから。ただしこの選択には代償がある。**施工した本人の実績が、
  元請けの中にしか残らない。**
- 掘る点その2: さらに調べると、同じ職人が複数の元請けに登録されると、システム上は
  別々の人として扱われる。つまり「この職人がこれまで何台やってきたか」を出す手段が無い。
  複数の元請けの下で動く働き方ほど、自分の実績が散らばる。
- 掘る点その3: これは自分自身の話でもある。全国を回って人の看板の下で作業してきた。
  その記録は、どこにも通算では残っていない。
- 一般化: 記録を残す仕組みを作るとき、「誰のための記録か」を決めないと、必ず誰かの分が
  こぼれる。今回こぼれたのは現場で手を動かした人の分だった。
- 公開区分: 公開可（具体的な取引先名・顧客名を出さない前提）
- 状態: 未執筆

## 権限の文字列を1つ変えると、経路が4つ動く — 「表を直した」は「全部直した」ではない
- 切り口: ある機能の権限を「一般ユーザーも可」から「運営のみ」に絞った。権限表を直し、
  処理側にも判定を入れ、**その判定を消すと落ちるテストまで書いて**出した。
  レビューで見つかったのは、**画面そのものが「ログインしているか」しか見ていなかった**こと。
  しかも自分は、その PR の説明文に「これは過去の失敗と同じ形だ」と**書いていた**。
  書いたのに、やった。
- ここに続きがある。**「だから権限の無い人にボタンが並ぶ」と書いたが、それも誤りだった。**
  実際には全画面を包む共通部品がブラウザ側で止めていた。自分は「その部品は存在しない」と
  思い込んでおり（存在しない関数名で検索して0件を得ていた）、**誤った前提の上に
  正しい修正を積んでいた**。修正自体は残す価値があった —— ブラウザ側の判定は
  データ取得が終わったあとに走るので、サーバ側に置くと取得そのものが起きない ——
  が、**説明していた理由は間違っていた。**
- 学び: 権限を表す文字列は、**それを読む場所の数だけ**波及する。メニュー、機能一覧、
  画面、処理 —— 別々の入口である。文字列を1つでも動かしたら、**その文字列を検索して
  参照元を全部数える**。これを最初にやれば5分で終わる。自分はこの検索を、
  出し終えたあとのセルフレビューで初めて打った。
- そしてもう一段: **正しい結論に達しても、理由が間違っていることがある。**
  修正が妥当だったので、前提の誤りは最後まで表に出てこなかった。
  「直った」は「分かった」の証明にならない。
- もう1つ: 「過去の失敗の型を名指しできること」と「その型を潰したこと」は別物である。
  失敗の台帳は、書き終えた説明文を飾るためではなく、**着手前に引く**もの。
- 公開区分: **公開可**（機能名・画面名・ロール名・人数は書かない。
  「権限を絞る変更は、表だけでは終わらない」の一般論として書ける）
- 状態: 未執筆

## 存在しない関数名で検索して、0件を「誰も使っていない」と読んだ
- 切り口: 「この設定表は誰が読んでいるのか」を調べようとして、表を読む関数の名前を
  **ファイルを開かずに思い出しで打ち込み**、検索した。0件だった。そこから
  「この表はどこからも参照されていない」と結論し、**社内の記録3か所と PR の説明文に
  事実として書いた**。実際には表はきちんと使われていて、全画面を包む共通部品が
  読んでいた。関数の名前が、自分が打ち込んだものと違っただけだった。
- 気づいたのは、別件で同じ場所を調べ直したとき。共通レイアウトを上から読んでいたら、
  その共通部品が目に入った。**最初に同じことをしていれば5分で分かった。**
- 学び: 検索で0件が出たとき、**「無い」と「探し方が違う」は同じ見た目をする**。
  0件は答えではなく、**検索語が正しいかを確かめろという合図**である。
  固有名詞で引くときは、その名前を**自分が実際に読んだ行から取る**。
  記憶から打つと、検索そのものが未検証の主張になる。
  AI に調べさせるときも同じで、**AI は自分が組み立てた検索語を疑わない。**
  「◯◯は存在しません」と言われたら、何という名前で探したのかを訊くとよい。
- 公開区分: **公開可**（機能名・画面名・件数の内訳は書かない。
  「0件をどう読むか」の一般論として書ける）
- 状態: 未執筆

## 営業資料を自動生成したら、社外に出す寸前で「別の画面」が載っていた
- 切り口: 提供資料の PDF に製品画面のスクショを差し込む仕組みを作り、撮影スクリプトを
  走らせた。スクリプトは3枚とも「✓ 撮影成功」と出した。**画像を開いて確認したら、
  2枚が別の画面だった。** 「新規発行」ボタンを名前で探す作りにしていたら、画面の別の場所に
  ある「**新規登録**（アカウント作成）」リンクに一致していた。つまり資料は
  「証明書の新規発行」というキャプションの下に**サインアップ画面**を載せる状態だった。
  気づいたきっかけは、2枚のファイルサイズがバイト単位で一致していたこと。
- 学び: 自動生成の成果物は「**処理が成功したか**」と「**正しいものが出たか**」が別物。
  スクリプトの ✓ は前者しか保証しない。**社外に出るものは、生成した本人が必ず現物を開く。**
  加えて、名前で要素を探す実装は「似た名前の別物」に静かに当たる —— 対象を名前で探さず、
  行き先を直接指定する形に変えた。
- 公開区分: **公開可**（画面名・実装の詳細は伏せ、「自動生成物は現物を見るまで完成ではない」
  という一般論として書ける）
- 状態: 未執筆

## 「デモ用アカウント」のつもりが、本番の全社データを見られる仕掛けが動いていた
- 切り口: 保険会社向けの閲覧権限に、名前がほぼ同じ2つのテーブルが並存していた。
  実際に使われているのは片方だけで、**もう片方には「全保険会社 × 全取引先」の
  組み合わせが自動で溜まり続けていた**。増えるたびに全組み合わせを付与する仕掛けが
  動いたままだったため。読んでいるコードが1行も無かったので誰も気づかず、
  実害も出ていなかった。ただし**その表を1行でも参照した瞬間に、全社のデータが
  全保険会社から見える**状態だった。
- もう1つ: そのテーブルは**変更履歴（マイグレーション）に一切登録されていなかった**。
  本番にだけ存在する。CI は「履歴からまっさらに作り直して動くか」を検査するので、
  **本番にだけ増えたものは構造上、検出できない**。
- 学び: 危険なのは「今壊れているもの」より「**壊れていないが、触れた瞬間に壊れるもの**」。
  実害ゼロは安全の証明にならない。そして、正規の手順を通さず本番へ入った変更は
  自動検査の外側に落ちる —— 検査が見ているのは正規の手順の方だから。
- 公開区分: **要確認**（一般論としては公開可だが、自社の権限設計・取引先数・
  保険会社名に触れずに書けるかは要検討。実データ露出の事故は起きていないが、
  書き方次第で誤解を招く）
- 状態: 未執筆

## 指示どおりにやると逆効果になるとき、AI は何をすべきか
- 切り口: 二重化したテーブルの片付けを頼まれ、「移行を完了させて」と指示された。
  着手前に前提を確認したら、**移行先とされた側は変更履歴に存在せず、コードからも
  参照ゼロ**で、中身は「全組み合わせ付与」だった。指示どおり移行すると
  **全社のデータが全保険会社に開放される**。方向が逆だった。
- そこで実行せず、根拠（履歴の有無・参照数・行数）を並べて前提が崩れていることを示し、
  判断を仰いだうえで**逆方向（不要な側を畳む）**に確定した。
- 学び: 指示の**言葉**ではなく**目的**に従う。ただし「勝手に逆をやる」のではなく、
  **なぜ言葉どおりだと目的に反するのかを、確かめられる事実で示してから聞く。**
  AI に作業させるとき、いちばん怖いのは「指示を忠実に実行できてしまう」こと。
- 公開区分: **公開可**（AI との協働で、指示の字義と意図がズレる場面の一般論として書ける）
- 状態: 未執筆

## 「見つからなかった」は「無い」ではない — 検査の出力を事実として読んだ話
- 切り口: 自社システムの権限チェック漏れを機械的に洗い出す検査を作り、その出力を見て
  「24箇所が無防備だ」と報告した。実際に1本ずつ中身を読んだら、**18箇所は別の書き方で
  守られていた**。検査は決め打ちの関数名を探す作りで、それ以外の書き方を認識できない。
  つまり出力の意味は「無防備」ではなく「**この検査が認識できない**」だった。
  同じ日に逆向きの誤りも見つかった。「守られている」と分類していた1箇所が実際は
  素通りで、閲覧専用の権限でも外部への公開操作ができた。
- 学び: 検査ツールの出力には**必ず2つの誤り方**がある。見落とし（守られていないのに
  出てこない）と誤検出（守られているのに出てくる）。片方だけ疑って安心するのが一番危ない。
  対策として、検査の一覧の**名前を「無防備な箇所」から「検査が認識できない箇所」に変え**、
  1件ずつ「なぜここは安全なのか」を書き添えた。**説明を書けない箇所はゼロになった。**
  名前を実態に合わせるだけで、次に読む人が同じ読み違えをしなくなる。
- 公開区分: **公開可**（具体的な画面名・ルート名・件数の内訳は書かない。
  「検査の出力をどう読むか」の一般論として書ける）
- 状態: 未執筆

## AI に開発させるとき、いちばん検証されないのは「AI が判断に使った道具」
- 切り口: AI に大規模な改修をさせると、**変更したコードは検証される**（テストが通る、
  型が合う）。ところが「**どこを変えるべきかを判断するために作った調査スクリプトや
  正規表現**」は、誰も検証しない。実例として、ロール名を正規表現で抽出したとき
  `admin` が `super_admin` の一部にも一致し、上位ロールの権限一覧を下位ロールのものとして
  読んでいた。その誤った表をもとに権限を決め、**実装と記録が食い違ったまま**進んでいた。
- もう1つ: 記録に**今日の日付を2日間違えて**書いていた。1コマンドで確かめられる事実だった。
  型チェックも自動テストも日付の中身は見ないので、「テストが全部通っている」ことを
  「検証済み」と取り違えていた。
- 学び: **テストが通ることは、テストが見ている範囲しか保証しない。** 日付・件数・
  固有名詞・出典はその範囲の外にある。対策として社内に**失敗の台帳**を作り、
  1件ごとに「なぜ気づけなかったか」と「再発防止（仕組みで止められないなら
  『仕組み無し』と正直に書く）」を必須にした。冒頭に失敗の「型」の表を置き、
  新しいミスをまず型に当てはめる。**直した内容は履歴に残るが、判断の誤り方は残らない。**
- 公開区分: **公開可**（AI 活用の実務論として書ける。社内の具体的な脆弱箇所は書かない）
- 状態: 未執筆

## 会社のお金を使う操作に「入口が2つ」あった — 片方だけ塞いでも意味が無い
- 切り口: 備品購入の権限を絞ったのに、**同じ注文を作るもう1つの入口**（決済方法が違うだけ）が
  開いたままだった。画面上はボタンの選択肢が分かれているだけで、裏では同じデータが作られる。
  1本ガードを入れて満足していたら、その操作を起こせる画面に他の入口が無いかを見ていなかった。
- 学び: 権限は「操作」ではなく「**結果として何が起きるか**」で数える。同じ結果を生む経路が
  複数あるなら、全部に同じ制限が要る。**ガードを1本入れたら、その操作を起こす画面に
  他の入口が無いかを必ず見る**——これを社内ルールに明文化した。
- 公開区分: **公開可**（「入口の数え方」の話として。具体的な画面・API 名は書かない）
- 状態: 未執筆

## 「テストが赤い」と「自分のせいで赤い」は別物 — 証明しないと、どちらの判断も間違える
- 切り口: 権限の穴を塞いだ変更を出したら、自動検査が赤くなった。原因は自分の変更ではなく、
  同じ時期に別の人が入れた変更だった。ここで取れる態度は3つある。「赤いから止める」
  「赤いけど自分のせいじゃないから通す」「**本当に自分のせいでないかを確かめてから通す**」。
- 掘る点その1: 2番目が一番危ない。「自分のせいじゃないはず」は、たいてい根拠がない。
  実際このときの検査ログは、末尾に別の検査の出力が出ていて、どれが落ちたのか読み取れない
  形になっていた（5つの検査を同時に走らせて、まとめて待つ作りだったため）。
  最初は「あの人の変更のせいだろう」と決めつけかけた。
- 掘る点その2: 自分の変更には**自分固有の落ち方**があり得た。61箇所に「この人はこの操作を
  していいか」という判定を足したので、検査されていない分岐が増える。検査率の下限を
  割る可能性があった。実際に測ったら余裕で通っていたが、測るまでは分からない。
- 掘る点その3: 最終的にやったのは、**合流後の状態を手元で作り直して、検査環境と同じ条件で
  全部走らせる**こと。落ちたのは相手の1ファイルだけ、自分の5298件は全部通った。
  ここまでやって初めて「自分のせいではない」と言える。所要20分。
- 一般化: セキュリティの穴を1日開けたままにするコストと、20分かけて根拠を作るコスト。
  **急ぐときほど、推測ではなく再現で判断する。**「たぶん大丈夫」で通した変更は、
  あとで何が起きても原因を切り分けられない。
- 公開区分: 公開可

## 「わからないところを、わからないまま安全にする」— 権限の穴を47箇所塞いだときに決めなかったこと
- 切り口: APIの認可漏れを塞ぐ作業で、47のルートに「この操作にはこの権限が要る」を書いた。
  ところが途中で、**権限の語彙そのものに無い操作**が出てきた。在庫の棚卸し、部品の発注、
  作業手順の定義。「証明書を発行できる人」は定義されているのに、「発注を出せる人」は
  誰も定義していなかった。機能を作るときに、権限の側を作り忘れていた。
- 掘る点その1: ここで一番やりたくなるのは、それらしい名前を発明することだった。
  `inventory:manage` と書けば、その場は綺麗に収まる。でもそれは「誰が棚卸しをするか」という
  **現場の決めごとを、コードを書いている人間が勝手に決める**ことになる。整備工場の運用は
  会社ごとに違う。1人でやっている店と、10人いる店では答えが違う。
- 掘る点その2: 代わりにやったのは、「閲覧だけの人は書けない」という**確実に言えることだけ**を
  ルールにして、それ以外は決めない、と決めたこと。決めていないことは「未決」と書いて残した。
  穴が空いたままより厳しく、決め打ちより弱い。中途半端に見えるが、後から正しく決め直せる。
- 掘る点その3: 似た名前の権限を流用しかけて、やめた話。「テンプレート管理」という権限が
  すでにあったので、作業手順テンプレートにもそれを当てようとした。名前は同じでも、
  証明書の雛形と作業手順は別のものだ。一度実装してから戻した。名前が似ているだけで
  結びつけると、後から「なぜこれが同じ権限なのか」を誰も説明できなくなる。
- 一般化: AIに実装を任せると、こういう「決まっていないこと」を平気で埋めてしまう。
  それらしい答えが出てくるので、埋まったことにすら気づかない。**現場の決めごとと、
  実装の都合を分けて、前者は必ず人に返す。**そのための線引きをどこに引くか。
- 公開区分: 公開可

## 「画面で隠している」は権限管理ではなかった — そして自分で書いた教訓を自分で踏んだ話
- 切り口: 管理画面には役職ごとの権限マトリクス（5役職×55権限）があり、役職に応じてメニューや
  ページを出し分けている。作った側の実感としては「権限は効いている」。ところが、その出し分けは
  ブラウザの中で動いていた。ページを隠しても、その裏側のAPIを直接呼べば通ってしまう。
- 掘る点その1: 実際に、証明書を「無効にする」操作の入口のひとつが、**会社に所属していること以外に
  何も確認していなかった**。閲覧専用の役職でも証明書を恒久的に無効化できた。そのファイルには
  「権限がありません」を返すための部品が読み込まれたまま一度も使われずに残っていて、チェックが
  失われた跡が化石のように残っていた。
- 掘る点その2: 「ではデータベース側の防御はどうか」と見に行くと、こちらも効いていなかった。
  後から入れた厳しいルールと、前からある緩いルールが**両方生きていて、緩い方が勝つ**仕様
  だったからである。厳しくしたつもりのルールが、隣の古いルールに黙って打ち消されていた。
- 掘る点その3（本題）: この穴を塞いだあと、「今後この見落としが起きないように」と自動検査を
  書いた。ところがレビューに回すと、**塞ぎ漏らした入口がまだ2つあった**。書いた検査が、
  それを見つけられなかったからである。原因は、検査が「無効化」という操作を**書き方**で
  探していたこと。同じ操作でも書き方が一つ違えば、検査は静かに0件を返し、テストは緑のまま通る。
  さらに悪いことに、**同じ日の一つ前の作業**で、自分が別の検査に「書き方は経路ごとに違うから
  リテラルで探すと漏れる」と書き残していた。それを読んだ上で、同じ失敗をした。
- 学び: 自動検査は「安心の根拠」になった瞬間に危険になる。緑であることは「検査が通った」こと
  しか意味せず、「網羅した」ことは意味しない。検査を書いたら、**わざと壊して落ちることを確認
  する**まではその検査を信用してはいけない。
- 公開区分: 公開可（一般化した技術・設計の教訓。具体的なエンドポイント名やテナント名は伏せる）

## 「無料プランのせい」というクレームが、実は無関係な原因だった
- 切り口: 利用者から「無料プランだと勝手にプラン画面に飛ばされる」というクレームが
  画面録画つきで届いた。文面通りに読めば「プランごとの制限ロジックのバグ」を疑う場面。
  だがフレーム単位で録画を解析すると、実際に飛ばされていたのは**証明書の画面ではなく
  設定（その他タブ）を開いたときだけ**で、しかも0.5秒で即座に起きていた。
- 掘る点: 原因を辿ると、プランとは無関係な「権限不足（RBAC）」の裏側の通信エラーを、
  グローバルな課金ガードが「課金の問題だ」と早合点して強制的にページごと連れ去っていた。
  利用者は「制限された＝プランのせい」と結論づけたが、実際は**役職の権限**の話で、
  有料プランのスタッフアカウントでも同じことが起きる状態だった。ユーザーの言葉をそのまま
  信じて「プラン判定」だけを疑っていたら、この不具合には辿り着けなかった。
- 公開区分: 要確認（技術的な教訓は公開可。クレームの原文・テナント名は伏せる）

## 自分のページ番号を間違えている営業資料

- 切り口: うちの資料ダウンロードは、PDF をファイルとして置いていない。
  料金も機能一覧も、アクセスされたその瞬間にコードから描いている。
  プランを1つ増やせば、資料も勝手に増える。差し替え作業がない。
  **その仕組み自体は狙いどおり動いていた。**
- ところが実際に8本を描き出してページ数を数えたら、3本がズレていた。
  料金プラン詳細はフッターに「5」と刷りながら、実物は6ページある。
  ROI テンプレートは「7」と刷って8ページ。機能紹介資料は「10」と刷って12ページ。
- 理由は単純で、各ページが自分の番号を文字列で持っていたから。
  「3 / 5」と書いてある。中身が A4 に収まらないと勝手に改ページが入る。
  オプションが1行増えただけで、その「5」は嘘になる。
  **中身を増やす自由と、総数を宣言する行為は両立しない。**
- 直し方は数字を6と8に書き換えることではなかった。**数えるのをやめた。**
  改ページの位置を知っているのは描画エンジンで、番号を宣言していたのは
  それを知らない側だった。知っている側に任せれば、この種のズレは起きなくなる。
- いちばん効いたのは、8本を実際に描いてページ数を数えるテストを書いたこと。
  PDF は開かない限り誰も見ない。**生成物は、生成しただけでは確かめたことにならない。**

## レビューの指摘が減らないとき、それは「バグが多い」ではない
- 切り口: あるモジュールに自動レビューが15件の指摘を出した。5件直した。すると**別のレビュアーが
  同じファイルに7件返してきた。**しかも直した箇所が新しい穴を1つ作っていた。
- 掘る点: ここで16件目を直しにいくと終わらない。**減らないこと自体が情報**で、
  「個々が間違っている」ではなく「**前提が違う**」の兆候だった。実際そのモジュールは
  「土台がこういう情報をくれる」つもりで書かれていて、土台は**その情報を持っていなかった**。
  二人のレビュアーが独立に同じ結論に着いたのが決め手になった。
- 公開区分: 公開可

## 足すのが危ないなら、消すのも同じだけ危ない
- 切り口: 「現場を知らないまま仕様を書き足すな」と決めた翌日に、逆の場面が来た ——
  明らかに前提が壊れているモジュールを、**消すべきかどうか**。
- 掘る点: 足すのを我慢したのと同じ理由で、消すのも我慢すべきだった。
  どちらも「自分が全体を分かっている」という前提に立っている。
  やったのは、前提と実際の食い違いを表にして**判断を持ち主に返す**こと。
  提案は出す、決めない。この線引きは、遠慮ではなく**射程の自覚**。
- 公開区分: 公開可

## 22本のプルリクエストが積み上がっていて、そのあいだ検査は一度も走っていなかった
- 切り口: 前の作業が、次の作業の土台の上に、そのまた次を積む形で22本の変更を
  重ねていた。一見きれいに並んでいる。だが自動検査は「本流に入る変更」だけを
  見る設定になっていて、**積まれている22本には一度も走っていなかった。**
  緑でも赤でもなく、**何も表示されていない**状態だった。
- 掘る点: 「動いているように見える」と「確かめた」の差。積み上げは進捗に見えるが、
  検査が届かない場所に置いた分だけ、確かめていない量が増えていた。
  1本ずつ本流に付け替えて押し直すと、そこで初めて検査が動き出す。
- 公開区分: 公開可

## 「?.」は、あなたが思っているほど守ってくれない
- 切り口: 状態遷移が正しいかを判定する関数に `table[from]?.includes(to)` と書いてあった。
  `?.` が付いているので、変な値が来ても安全に false を返す —— と読める。
  実際には `"toString"` を渡すと**例外を投げて落ちる**。
  JavaScript のオブジェクトは、書いた覚えのないキーをいくつも持っているから。
- 掘る点: 型で `"予定" | "作業中" | ...` と絞ってあっても、実行時に来るのは
  利用者のブラウザから送られてきた**ただの文字列**。型は設計図であって門番ではない。
  同じフォルダの別のファイルは、既にこれを避ける書き方をしていた ——
  **正解が隣にあるのに、書き写されていなかった。**
- 公開区分: 公開可

## 「表に無い」を「終わっている」と答えるコード
- 切り口: 案件が終了状態かを判定する関数が、**知らない状態を渡されると「終了」と答えて**
  いた。表に載っていない＝遷移先が無い＝終端、という素直すぎる実装。
  問題は、いま稼働しているシステムが使っている値が、まさにその「表に無い値」だったこと。
  つまり**現役の案件が、片っ端から「完了済み」と判定されうる**状態だった。
- 掘る点: わからないことを「無い」と答えるか「わからない」と答えるかの違い。
  前者は静かで、後者はうるさい。うるさいほうが安全なことは多い。
- 公開区分: 公開可

## 守るはずの検査が、エラーを握りつぶして「異常なし」と言っていた
- 切り口: 変更を送信する前にテストを走らせる仕組みが入っていた。ところがある条件では
  内部のコマンドが失敗し、その失敗が握りつぶされ、出力が空になり、
  空を「変更なし」と解釈して**テストを丸ごと飛ばしたうえで「文書だけの変更です」と表示**
  していた。赤くもならず、警告も出ない。
- 掘る点: 失敗したときに**止まる**のか**通す**のかは、仕組みを作るときの一番大事な選択。
  ここは通す側に倒れていた。しかも通したことを、成功と同じ見た目で報告していた。
- 公開区分: 公開可

## 「同じ規則」と書いたコメントが、参照先ごと消えていた
- 切り口: 同じ計算が2箇所にあった。データベース側と、アプリ側。
  どちらのコードにも「もう片方と同一でなければならない」とコメントが書いてある。
  親切だ。ところが**参照先のファイルは存在しなかった。**番号が違っていて、
  そのファイルはどこにもない。つまり誰も突き合わせようがなかった。
- 実際にズレていた。データベースは小数第2位で丸めてから区分を決め、
  アプリは丸めずに決めていた。ほとんどの車では同じ答えになる。
  境界のすぐ下、0.005 の幅に入った車だけ、**別の区分＝別の価格帯**になる。
  現存するデータには1台も居なかった。次に登録される車が最初の1台になる。
- 直し方は「コメントを正しい番号に直す」ではなかった。**機械に突き合わせさせる。**
  データベースの定義文をテキストとして読み、そこから規則を抜き出して、
  アプリの実装と同じ答えになるか確かめる。データベースを起動しなくてもできる。
  片方だけ変えれば落ちる。
- そして、その検査自体が最初は**効いていなかった。**書いた本人は通ったので
  満足していたが、レビューに「わざとズラしてみろ」と言われて試したら、
  **ズラしても緑のままだった。**3通りの抜け道があった。
  検査は「通ること」ではなく「壊したときに落ちること」で確かめるしかない。
- 学び: 取り決めを人の記憶とコメントで守らせない。守らせる仕組みを書く。
  そして**その仕組みが本当に噛むかを、壊して確かめる。**
- 公開区分: 公開可（車両情報・テナント名・接続情報は出さない）

## エラーメッセージが指した場所を、確かめずに動かした
- 切り口: 自動化が止まり、エラーが「このファイルの順番がおかしい」と1本を名指しした。
  名前を変えた。次も止まった。今度は「そのファイルが見当たらない」と言われた。
  **名前を変えたファイルは、実はもう本番に反映済みだった。**
  順番を直したつもりで、存在しないことにしてしまった。
- エラーメッセージは親切だった。ファイル名も、打つべきコマンドも書いてあった。
  ただし**そのファイルが既に適用済みかどうかは書いていなかった**。
  書けるはずがない。それを知っているのは本番だけで、エラーを出した側は
  手元の情報しか見ていない。
- 自分も同じ穴に落ちた。「本番を確認した、入っていない」と報告したが、
  新しい順に3件だけ見ていた。探していたものは4件目より下にあった。
  **番号が小さいことと、存在しないことは違う。**「無い」を示すには、
  名前で名指しして引くしかない。
- 同じ形の失敗が2回起きている。2回とも、直そうとした手が次の障害を作った。
  そして2回とも、その手を選ばせたのは**手順書に書いてあった一文**だった。
  「順番がおかしければ名前を変える」。正しい。ただし「まだ反映されていなければ」
  という条件が抜けていた。抜けた条件は、読む人には見えない。
- 学び: エラーが名指ししたものを動かす前に、**動かしていい状態かを別の情報源に聞く。**
  一覧の上から数件を見るのは「確認」ではない。
- 公開区分: 公開可（接続情報・製品固有の識別子は出さない）

## アプリのバグだと思っていたら、サーバが1週間前のままだった
- 切り口: 現場から「決済画面でボタンを押しても何も起きない」「QRコードが出ない」と
  報告があった。アプリを疑って直した。原因はアプリではなかった。
  **サーバに、直したコードがそもそも届いていなかった。**
- デプロイが止まっていた。ただし「失敗していた」のではない。**何も起きていなかった。**
  エラーの記録も、キャンセルの記録も、1行も残っていない。失敗していれば赤い印が
  付いて気づける。記録が作られないと、気づく手がかりがない。
  1週間気づかず、その間に9回の変更が宙に浮いていた。
- しかもアプリ側とサーバ側は、この間に「食い違って」いた。新しいアプリは
  「この情報はサーバが自分で決めるべきだから送らない」に変えてあった。
  古いサーバは「その情報は必須」のまま。届かないので突き返す。
  どちらも単体では正しく、組み合わせだけが壊れていた。
- 学び: 動いていることは、動いている証拠にはならない。**止まったことが記録に残るか**を
  先に確かめておく。「失敗したら分かる」仕組みは、「何もしない」を検知できない。
- 公開区分: 公開可（ドメイン構成・接続情報・サービス名の内部設定は出さない）

## 安全のために付けた条件が、機能を丸ごと殺していた
- 切り口: 現場から「作業画面から写真を撮ろうとすると『先に証明書を作ってください』と
  言われる。作っても、戻るとまた同じことを言われる」と報告があった。無限ループ。
  調べると、証明書が作業案件に**紐づいていなかった**。45件すべて。
- 原因は、取り違えを防ぐために付けた条件だった。「証明書の車両とお客様が、
  案件の車両とお客様と一致すること」。まっとうに見える。ところが実データでは、
  **案件の側に車両もお客様も入っていない**ことがほとんどだった（169件中164件）。
  条件は「空っぽ」を「一致しない」と判定して、ほぼ全部を弾いていた。
  安全装置が、守るはずの機能そのものを止めていた。
- しかも最初の修正は外れだった。アプリ側が案件番号を送っていないのを見つけて直し、
  「これで直った」と報告しかけた。**実データを数えて初めて**、本当の原因が
  サーバ側の条件だと分かった。1回目の修正は正しかったが、足りていなかった。
- 学び: 「一致しなければ弾く」を書くときは、**空っぽをどう扱うかを必ず決める**。
  空は不一致ではない。そして、条件を書いたら実データで何件通って何件落ちるかを
  数える。エラーも出ずに静かに0件を返す作りは、動いているように見えてしまう。
- 公開区分: 公開可（件数の内訳・顧客情報・会社名は出さない）

## 直そうとして、同じ壊し方をもう一度した
- 切り口: 自動化が止まった。原因を直して再開したら、また止まった。二度目のエラーを
  直したら、三度目も止まった。三度目の原因は**私が二度目の修正でやったこと**だった。
  順番の問題を直すために7個のファイルの名前を変えたのだが、そのうち1個は
  **すでに本番へ反映済み**で、名前を変えてはいけないものだった。
  結果、一度目とまったく同じ「本番にあるはずのものが見当たらない」状態を
  自分の手で作り直した。
- なぜそうなったか: エラーメッセージが「この7個を直せ」と親切に教えてくれたので、
  そのまま7個を直した。**その7個が本当に全部直してよいものかを、本番側に問い合わせて
  確かめなかった。** エラーは「今わかっていること」しか書けない。書いていないことは
  自分で調べるしかない。
- 四度目でようやく通った。通る前にやったのは、本番の記録422件と手元のファイル一覧を
  1件ずつ機械的に突き合わせること。最初からこれをやっていれば一度で済んだ。
- 学び: **失敗の指示に従うのと、失敗の原因を理解するのは違う。** 前者は速く、
  たいてい途中まで正しい。途中までしか正しくないと、残りが次の失敗になる。
- 公開区分: 公開可（システム名の詳細・接続情報は出さない）

## 便利な近道が、あとで自動化を壊す
- 切り口: データベースの変更を本番に反映する仕組みは自動化してある。ところが急ぐときに、
  その仕組みを通さず直接あてる近道を何度か使っていた。結果は動いた。問題も出なかった。
  だが数日後、まとめて反映しようとしたら**自動化そのものが止まった**。
  直接あてた分は本番の台帳に記録が残る一方、手元の記録には残らない。
  仕組みは「両方の記録が一致していること」を前提に動いていたので、
  食い違いを見つけた時点で、何もせずに停止した。
- 幸いだったのは、**止まり方が正しかった**こと。中途半端に反映してから壊れるのではなく、
  作業を始める前に照合して、一致しなければ何もしない。本番のデータは無傷だった。
  怖いのは止まったことではなく、止まったまま気づかないこと —— 実際、
  以前これで3週間ほど反映が止まり、機能がひとつ丸ごと動かなくなったことがある。
- 学び: 自動化を飛ばした近道は、**その場では成功して見える**。代償は後払いで、
  しかも払うのは別の日の自分だ。近道を使うなら、使った直後に帳尻を合わせるところまでを
  ひとつの作業にする。あと、こういう前提は仕組みの中にコメントで書いてあった。
  書いてあっても、急いでいるときには読まない。
- 公開区分: 公開可（接続情報・システム名の詳細は出さない）

## 選択肢が1つしかない「選んでください」は、作らないほうが親切だ
- 切り口: 「パソコンから作ったデータに店舗が入っていない」という問題が見つかった。
  素直に考えれば、作成画面に「店舗を選ぶ」欄を足せばいい。実際そう書き留めていた。
  ところが数えてみると、**使っている会社はどこも店舗を1つしか持っていなかった**。
  選択肢が1つしかない選択欄を、全部の作成画面に足すことになる。押させる意味がない。
  そこで欄は作らず、**サーバー側で「店舗が1つなら、それ」と決める**ようにした。
  2つ以上ある会社が現れたら推測はせず、空のままにする。そのとき初めて欄を作る。
- もう一つの発見: 同じ場所を直している途中で、**スマホから送られてきた店舗番号を
  誰も検証していなかった**ことが分かった。細工すれば、よその店の売上として
  記録できてしまう状態だった。直す場所を1つにまとめたので、そこも同時に塞がった。
- 学び: 「入力欄を足す」は一番わかりやすい解決策だが、**その欄を人が使うのかを
  先に数える**。数えたら不要だった、はよくある。そして、同じ判定を何箇所にも
  手書きしていると必ずどこかが抜ける —— 実際、9箇所のうち1箇所にだけ検証があった。
- 公開区分: 公開可（会社名・店舗名・件数の内訳は出さない）

## 買う前に、困っているかを確かめる
- 切り口: カード決済用の専用端末（リーダー）を入れるかどうかを検討した。
  入れれば現場の手順は数秒短くなる。ただし、その端末で番号を手入力する機能が
  日本で使えるのかは、調べても確認できなかった。費用も、何台どの店に置くかも決まらない。
  一方、前日に入れた「決済会社の画面をその場で開く」方式は、追加費用ゼロで
  既に動いている。結論は「いまは買わない」。困ってから確認する。
- 学び: 導入を迷ったら「**それが無いと今どれだけ困っているか**」を数字で出す。
  出せないなら、まだ買う時ではない。ちなみに「タッチ決済が読めなかった」が
  実際に何件起きているかは計測していなかった。次に迷わないよう、そこは測る。
- 公開区分: 公開可（費用・取引先名は出さない）

## 「その機能を作ってくれ」に、作らずに応える
- 切り口: 現場から「タッチ決済がダメだった時のために、カード番号を打てるようにしてほしい」
  と言われた。しかしカード番号を自社の画面で受け取るには、カード業界の重い認定が要る。
  小さな会社が背負うものではない。
  では断るのかというと、そうではなかった。よく見ると、**カード番号を手で入力して
  決済する仕組みは既に動いていた** —— QR決済がそれで、お客様が決済会社の画面に
  番号を入力している。足りなかったのは機能ではなく、**タッチが失敗した瞬間に
  そこへ移る道**だけだった。ボタン1つで済んだ。
- もう一つの発見: その経路を調べている途中で、**予約からの QR 決済が売上として
  1件も記録されていない**ことが分かった。カードは切られ、お客様は払い終わっている。
  なのに帳簿に残らない。要望に応えるために覗いた場所から、別の穴が出てきた。
- 学び: 言われた通りに作る前に、**何に困っているのか**を一度言い直す。名指しされた
  解決策が作れなくても、困りごとの方は既にある道具で解けることがある。
- 公開区分: 公開可（テナント名・金額・個人情報は出さない）

## 「データが消えた」の正体は、誰も書いていない欄で絞っていたこと
- 切り口: 現場から「証明書が1件あるはずなのに出てこない」と言われた。データベースを
  直接見ると、証明書はちゃんとあった。消えていたわけでも、保存に失敗していたわけでもない。
  原因は、アプリ側が「店舗」で絞り込んでいたこと。そして**その店舗欄に値を入れている
  作成画面が、片方（スマホ）にしか無かった**。パソコンから作った分は店舗が空のまま。
  店舗を選んでいる端末からは、空のものが1件も見えない。
  数えたら、本番のデータは 225 件すべてが空だった。つまりこの絞り込みは、
  作った日から一度も正しく動いていなかった。誰も気づかなかったのは、
  **エラーが出ないから**。画面は「まだありません」と、いつもの顔で出る。
- 学び: 動かない機能はエラーを出すが、**間違った条件で絞った機能は静かに0件を返す**。
  0件はエラーより怖い。「無い」のか「見えていない」のかが画面から判別できない。
- 公開区分: 公開可（テナント名・個人名・具体的な顧客情報は出さない）

## 「全部直す」を諦めたら、はじめて前に進んだ
- 切り口: データベースの設計変更の履歴が、空の状態から流し直すと3分の1以上失敗する
  状態だった。全部通るようにしようとして原因を1つずつ辿ったら、**そもそも直せないもの**が
  混ざっていた。一度も存在しなかったテーブルを参照している古い変更が残っていて、
  それは当時も失敗していた。過去の記録を書き換えると本番との突き合わせが壊れるので、
  手を出せない。
  そこで目標を変えた。「全部通るようにする」ではなく「**通らないものを増やさない**」。
  通らない9件を名前で一覧にして、増えたら自動で気づくようにした。
  達成できない目標を掲げていた間は達成度が測れなかったが、測れる目標に変えた途端に
  171件が9件まで減った。
- 想定読者: 古いシステムを引き継いだ人 / 技術的負債の返し方に悩んでいる人
- 状態: 未着手
- メモ: 同じ回に「棚卸しは件数ではなく詰まっている理由で分類する」も書ける。
  65件の宿題を整理したら、手を動かせば片付くのは7件だけで、
  残り43件は判断・実測・権限が要るものだった。件数だけ見ると「まだ50件も」と読めるが、
  実際には「自分で進められるのは7件」というのが正しい読み方。
  ※ 具体的なテーブル名・関数名・テナント情報は書かない。

## 「権限を絞る前に、絞られる側を数える」——そこで別のバグが出てきた
- 切り口: 誰でも呼べてしまう危ない処理があり、呼べる相手を絞ることにした。
  絞る作業でいちばん怖いのは「絞りすぎて現場が止まること」なので、
  その処理を**呼んでいる側**を全部数え直した。すると、前回「全部直した」と
  思っていた範囲の外（スマホアプリ側）にまだ3か所残っていた。
  さらにその3か所を読み直したら、**カード決済を1回すると売上が2件記録される**
  作りになっていたことが分かった。決済の部品が内部でもう売上を作っているのに、
  画面側がもう一度作っていた。権限の話とは無関係のバグだが、
  呼び出し元を数えたから見つかった。
- 想定読者: 業務システムを運用している人 / セキュリティ対応で権限を絞る人
- 状態: 未着手
- メモ: 「止めていいかどうかは、感触ではなく実データで決める」も同じ回に書ける。
  この機能は5か月間まったく使われていないことがデータで確認できたので、
  止まる利用者が居ないと判断して適用できた。
  ※ 具体的な関数名・攻撃手順・テナント情報は書かない。

## 「ライブラリが『HEIC です』と言うのに、中身は JPEG だった」
- 切り口: スマホで撮った写真を送る機能。写真ライブラリの部品が「この画像は HEIC です」と
  教えてくれるので、それを見て「HEIC は送れません」と弾く実装にした。ところが Android では、
  その部品は**元ファイル**の種類を返すだけで、実際に書き出されるファイルは圧縮の過程で
  JPEG に変換されている。つまり「JPEG なのに HEIC と言われて弾かれる」状態になっていた。
  部品が名乗るメタデータより、実際に出来上がったファイルの拡張子を見る方が正しかった。
- 想定読者: スマホアプリを作っている人 / 外部ライブラリの戻り値を信じる前に確かめたい人
- 状態: 未着手
- メモ: 同じ回に「上限は仕様書の数字ではなく、実際に詰まる場所に合わせる」も書ける。
  送信サービス側の上限は10MBだが、その手前にサーバ側の受け取り上限（約4.5MB）があり、
  10MBで実装すると必ず手前で切られる。

## 「同じ処理を2か所に書いた瞬間、片方だけ直る日が確定する」
- 切り口: 請求書を「確定」すると、内容の改ざん検知用の封印が付き、設定によっては顧客へ
  自動送付される。この確定ボタンをスマホにも付けるとき、パソコン版の処理をコピーせず
  1つの関数に抜いて両方から呼ぶようにした。コピーすると、後で封印の仕様を直した人が
  片方を直し忘れ、「確定したのに担保が無い書類」が静かに生まれる。
  法令対応のように、抜けたことに後から気づいても戻せない処理は、入口を1つにする構造
  そのものが対策になる。
- 想定読者: 業務システムを作っている人 / 電子帳簿保存法まわりを実装する人
- 状態: 未着手
- メモ: 「読み取りは直接データベースを見る、副作用のある操作だけサーバを通す」という
  線引きの話もセットで書ける。

## 「そのライブラリ、入れる前に読んでますか」——足さずに済んだ生体認証
- 切り口: アプリに指紋認証を足したい。普通は専用のライブラリを入れる。でもモバイルアプリの場合、
  ライブラリを1つ足すとアプリ本体を作り直すことになり、テスト中の手元のアプリが使えなくなる。
  そこで、鍵の保管にもう入れてあるライブラリの説明を最後まで読んだら、「この値を読むときは
  指紋認証を要求する」という設定が最初からあった。足さずに済んだ。
  ライブラリを入れる判断は、コードの行数ではなく「作り直しのコスト」で決まる。
- 想定読者: モバイルアプリを作っている人 / 依存を増やすか迷う開発者
- 状態: 未着手
- メモ: 「機能を足す前に、既にあるものを読む」は規約表示・ナレッジ共有に続いて3回目の教訓。
  シリーズにできる。

## 「起動時だけロック」では何も変わらなかった話
- 切り口: 「一度ログインすると次から素通りだから認証を挟みたい」と言われた。言われたとおり
  起動時に認証を入れると、実は体感が変わらない。スマホはアプリを長く生かしておくので、
  完全な起動なんて滅多に起きないから。実際に必要だったのは「しばらく離れて戻ってきたとき」。
  言われた文面を満たすことと、言われた問題を解くことは違う。
- 想定読者: 要望を受けて作る立場の人全般 / AIに実装を任せている人
- 状態: 未着手
- メモ: 「何分離れたら再認証か」の線引きは現場の実感次第で、今は5分の仮置き。
  正解が数字で決まらない設計判断の例としても書ける。

## 「画面の一番上に画面名を出すのをやめた」——現場が最初にやるのは“探す”だった
- 切り口: アプリの各画面の一番上には画面名を出すのが普通だと思っていた。でも下のタブバーの
  アイコンで今どこにいるかは既に分かっている。同じ情報を、画面幅いっぱいの帯を使って2度出していた。
  現場の人が画面を開いて最初にやるのは「ナンバーで探す」。だから一等地は検索窓に渡した。
  情報を足すのではなく、重複している方を消して空いた場所を渡す、という直し方。
- 想定読者: 現場向けの業務アプリを作っている人 / UIの引き算に悩む人
- 状態: 未着手
- メモ: 併せて「押しても何も起きないボタン」を撤去した話も同じ回に入る。見た目は完成しているのに
  何も起きない要素は、無いのと同じではなく“壊れている”のと同じ。

## 「英語の月表記が読めない」は仕様バグだった
- 切り口: 日付選択のカレンダーが "Aug" と英語で出ていた。開発者は気にも留めないが、
  現場の年配の方には読めない。端末の言語設定に任せていたせいで、こちらの想定外の表示になっていた。
  「既定に任せる」は楽だが、誰が使うかが決まっている業務アプリでは、既定こそ疑うべきところ。
  修正は1行（表示ロケールの固定）。気づくまでが全て。
- 想定読者: 業務アプリ開発者 / 現場ヒアリングの価値を語りたい人
- 状態: 未着手
- メモ: 代表の実機テストで出た指摘。開発環境では日本語表示になっていて再現しなかった類の話として書ける。

## 「アプリの中で規約が見れない」——サーバーに置いた正解が、配信の時差で壊れた話
- 切り口: 利用規約をアプリ内で表示するとき、文面を二重管理したくないのでサーバーAPIから取る設計にした。
  設計としては正しいのに、実機では真っ白。理由は単純で、そのAPIはまだ配信されていなかった。
  アプリとサーバーはリリースの時期が別なので、片方にしか無いものに依存した瞬間に壊れる。
  結論は「同梱する。ただしコピーが本体とズレたらテストが落ちるようにする」。
  単一定義源は“1箇所に置く”だけでなく“コピーの一致を機械で保証する”でも守れる。
- 想定読者: モバイルアプリとWebを両方持つSaaSの開発者 / 小さく速く作る人
- 状態: 未着手
- メモ: おまけの教訓として、本文をプログラムで抽出したとき正規表現が27項目中14項目しか拾えておらず、
  法務文書が半分欠けかけた件も材料になる（「動いたように見える抽出」の怖さ）。

## 「作る前に既存のアクセス権を読んだら、欲しい仕組みが既にあった」
- 切り口: 「他社が蓄積したノウハウを現場で見られるようにしたい」と言われて、共有の仕組みを
  新しく作ろうとした。その前にデータベースのアクセス権設定を読んだら、「公開済み」のものは
  最初から全社横断で読める設計になっていた。作るべきは仕組みではなく画面だけだった。
  新機能の要望が来たとき、まず「もう出来ることではないか」を確認する。
- 想定読者: 小さく速く作るSaaS開発者 / AIに実装を任せている人
- 状態: 未着手
- メモ: 「公開するかどうかは投稿者が選ぶ」という設計も既にあり、同意の取り方を新しく
  決める必要が無かった点も材料。テーブル名・内部の権限名などは書かない。

## 記入フォーマット

```
## タイトル案
- 切り口: 何を伝える記事か
- 想定読者: 誰向けか
- 状態: 未着手 / 執筆中 / 執筆済み（リンク）
- メモ: 材料になりそうな情報・データ
```

## 入口は増えるが出口は増えない ― 必須項目をどこで強制するか
- 切り口: 必須入力を「作成画面ごと」に足していったら、5本ある作成経路のうち2本を漏らした。
  しかも自分では「全経路に入れた」と書いていて、レビューで指摘されて初めて気づいた。
  作り直すときに数え直したら、作成経路は5本あって今後も増えるのに対し、
  **「発行済み」になる道は2本しかなく、しかも増えない**という非対称に気づいた。
  入口ではなく出口に1つ置いたら、来月新しい作成経路が生えても漏れない。
  同じ場所に「写真が1枚も無ければ発行できない」という先行ルールが既にあり、
  結果的に形が揃った ―― 先に正解を書いていた自分に後から追いついた格好。
- 想定読者: SaaS のPM・プロダクト設計者、バリデーション設計をする開発者
- メモ: 「必須項目をどこに置くか」は入力体験の話に見えて、実は**状態遷移の設計**の話。
  入口の数と出口の数を数えるだけで、どちらに置くべきかが決まる。
  「全部の経路に入れました」と書いた自分の言葉が誤りだったことも含めて書くと強い
  ―― 網羅を主張するときは、数えたのか思い込みなのかを区別する必要がある。
  実数（本番の件数）は書かないこと。テナント名・顧客情報も出さない。
- 状態: 未着手
- 公開区分: 公開可（本番の実データ件数・テナント名は非公開）

## 復元元が無い過去データは、推測で埋めない
- 切り口: 走行距離を必須化したあと、それ以前に発行済みの証明書には値が無い状態が残った。
  写真は残っているが施工写真であってメーターは写っていない。作業記録にも無い。
  「初度登録からの年数×平均年間走行距離」で埋めることは技術的にはできたが、やらないと決めた。
  証明書は証拠であって、推測値が1つ混じった瞬間に**残り全部の信頼も落ちる**。
  空欄は「分からない」と正しく伝えるが、推測値は「分かっている」と嘘をつく。
  代わりに、当時の紙の作業記録が残っている案件だけ、既存の編集APIから入れられるようにした
  ―― 判る分だけ入れて、判らないものは空のまま。
- 想定読者: データ基盤・データ品質に関わる人、中古車業界の事業者
- メモ: 業界の文脈と接続できる。メーター巻き戻しが問題になる業界で、
  自分たちが推測値を入れたら同じことをしていることになる。
  遡及入力のための専用画面は作っていない（既存の編集APIがそのまま経路になった）ことも、
  「作らずに済ませる」話として添えられる。実数は書かないこと。
- 状態: 未着手
- 公開区分: 公開可（本番の実データ件数・テナント名・顧客情報は非公開）

## 「任意入力」は入力されない ― 4つの機能が入力ゼロの上に建っていた話
- 切り口: 走行距離を「記録すると次回整備時期の判定に活用できます」という任意の付加情報として作っていたら、
  本番の入力実績がゼロだった。テーブルもDBトリガーも入力欄も揃っていて、配管は完璧に繋がっていた。
  入口で水が入っていなかっただけ。しかもその上に整備リマインダー・劣化予測・車両パスポートの走行距離履歴・
  残価判定という4つの機能が乗っていた。発見のきっかけは新規事業の検討で「実際に何件あるのか」を数えたこと。
  機能を作ることではなく、実数を見にいくことがバグを見つける、という話は前回のVINの件と同じ構図で書ける。
  「任意にした瞬間、その項目は存在しないのと同じ」という設計上の教訓に着地させる。
- 想定読者: SaaS のPM・プロダクト設計者、フォーム設計をする開発者
- メモ: 任意にした理由は「入力の手間を増やしたくない」という善意だった。
  でも走行距離は車が来た時点でメーターを見れば分かる値で、手間はほぼゼロ。
  「手間」を過大評価して「データが無い」という遥かに大きなコストを払っていた。
  修正は保存先も既存の配管を再利用したのでマイグレーションなし。実数は書かないこと。
- 状態: 未着手
- 公開区分: 公開可（本番の実データ件数は非公開）

## 「信用回復ローン」で信用は回復しない ― 業界の言葉と中身がズレていた話
- 切り口: 中古車の自社ローンを「信用回復ローン」と呼ぶ商品が市場に多数あるが、販売店は指定信用情報機関の加盟会員ではないため、
  支払い実績が信用情報に登録されない。つまり完済しても信用情報は回復しない。
  自動車業界の信頼低下という問題意識そのものが、金融の側でも同じ形で現れていた、という切り口で書ける。
  「では本当に信用が回復する商品をどう設計するか」まで含めて、名前と中身を一致させることが自分たちの仕事だという話に着地できる。
- 想定読者: 車を買いたいが審査に通らない層 / 中古車販売・整備の事業者 / フィンテック関心層
- 状態: 未着手
- メモ: 事故情報の登録期間はCIC・JICCが最長5年、KSC（全銀協の官報情報）が最長7年（2022年11月に10年から短縮）。
  利用者の不満は総支払額の不透明さ・GPSでの遠隔引き揚げ・保証範囲の争いに集中している。
  **パートナー候補名と自社の実データ（登録台数等）は書かないこと。**
- 公開区分: 公開可（ただし上記メモの注記を守ること）

## 一度きりのバックフィルは、静かに腐る ― 4か月分のデータが機能に届いていなかった話
- 切り口: 新しい列を追加してバックフィルするマイグレーションを書いたが、「これから入るデータを誰が埋めるのか」を決めていなかった。
  結果、バックフィル以降に入力された車体番号は4か月にわたって一件も機能に反映されず、しかもエラーは一切出なかった。
  発見のきっかけは新規事業の検討で「実際に何台あるのか」を数えたこと。機能を作ることではなく、実数を見にいくことがバグを見つけた。
  書き込み経路が5箇所あったので、呼び出し元を1つずつ直すのではなくDBトリガー1本に寄せた設計判断も書ける。
- 想定読者: Supabase / PostgreSQL を使う開発者、SaaSのPM
- 状態: 未着手
- メモ: 「ADD COLUMN + 一度きりのUPDATE」は、その列を書き込み経路の数だけ埋め忘れる余地を残す。
  導出できる値なら生成列かトリガーで、DB側に責任を持たせるほうが漏れない。
  マイグレーションに自己検証（壊れていれば例外で落ちる）を同梱する話も添えられる。
  実数（台数）は書かないこと。
- 公開区分: 公開可（実データの台数は非公開）

## API層は通るのにDB層で弾かれる — マルチテナントSaaSの権限二重管理の落とし穴
- 切り口: Next.js API層の権限チェックとSupabase RLSの権限チェックが別系統になっていて、片方だけ通るバグが潜在していた話。super_adminロールがAPI層では全権限を持つのにDB層では認識されず、全テーブルの書き込みがブロックされていた。ヘルパー関数1箇所の修正で70+のRLSポリシーに一括適用できた設計判断。
- 想定読者: Supabase / マルチテナントSaaSの開発者
- 状態: 未着手
- メモ: 権限チェックの二重管理（アプリ層 vs DB層）はマルチテナントSaaSの定番の落とし穴。RLSヘルパー関数を一点修正する方式 vs 全ポリシー修正する方式の比較。
- 公開区分: 公開可

## 候補（CLAUDE.md の代表者経歴・問題意識から）

## なぜ「整備記録SaaS」を無料にしても真似されないのか — DeepSeekに学ぶ自動車業界の堀の作り方
- 切り口: DeepSeekが「コストが安すぎて真似できない」で堀を作ったのと同じ構造が、
  自動車整備の施工記録にもある。ブロックチェーン記録は過去に遡って生成できないので、
  「方法を公開しても結果を独占できる」。技術のオープン化が参入障壁になるという逆説。
- 想定読者: SaaS経営者 / スタートアップの競争戦略に関心がある人 / 自動車業界のDXに
  関心がある人
- 状態: 未着手
- メモ: 「方法を公開し、結果を独占する」がキャッチコピー。具体的な数値（記録コスト0.15円、
  ネットワーク効果の臨界点）は非公開。堀の概念とDeepSeek並行論に絞って書く。
  公開区分: 公開可

## 12店舗のSaaSが「単価を上げるべきか、キャッシュポイントを増やすべきか」を試算して出た答え
- 切り口: 「キャッシュポイントを増やす vs 単価を上げる」は定番の議論だが、店舗数が12しか
  ない段階では両方とも意味がなかった。母数が小さいうちは掛け算の係数をいじっても
  インパクトが出ない。答えは「どちらでもなく店舗数を増やせ」だった話。
  5つのモデルを実際にシミュレーションして比較した過程も含めて書ける。
- 想定読者: 初期ステージのSaaS経営者 / プライシングに悩んでいるスタートアップ
- 状態: 未着手
- メモ: シミュレーションの結果グラフは使える（匿名化済み）。具体的な価格・ARPUは
  レンジで記載（正確な数字は非公開）。
  公開区分: 公開可（具体的な価格は丸めた数字で）

## 「答えて終わり」のチャットボットをやめた — LINE自動応答に”次の一歩”のボタンを足した小さな改善
- 切り口: 店のLINEをAIで自動応答させても、「営業時間は9〜18時です」で会話が終わってしまえば予約や見積りには繋がらない。そこで、AIがFAQに答えた直後に「お見積りをお願いしたい」「スタッフに相談したい」のボタンを一緒に出すようにした。ボタンを押すと、見積りの会話フロー（車検証を送ってもらって正式見積りへ）が始まるか、スタッフに引き継がれる。ポイントは”ゼロから作らない”こと——ボタン送信も、押された後のフローも、既に別機能で作ってあった部品を繋いだだけ。DBもいじらず、既にその機能を使っている店だけボタンが出て、使っていない店は今まで通り。もう一つの学び:「チャットボット化したい」という曖昧な要望を、”答えられる内容を増やす（＝店がFAQを登録するデータ運用）”と”答えた後の導線を増やす（＝コード）”に分けたこと。混ぜると際限がないが、分ければそれぞれ適任者に渡せる。
- 想定読者: 店舗でLINE公式アカウントを運用する事業者、AIチャットボットの導入で「導入したけど成果に繋がらない」と感じている人、小さく確実に改善したい開発者。
- 状態: 未着手
- メモ: 公開可（UX設計・「回答したら次の行動をボタンで示す」「要望を”データ運用”と”コード”に分解する」という考え方のみ）。テナントID・本番データ・プラン内部設定は載せない。キーメッセージ:「チャットボットの価値は”答える”ことより”次の一歩に繋ぐ”こと」「新機能は既存部品の接続で作れることが多い（最小差分）」。

## 本番稼働中の SaaS に新しい仕様書が来たとき、コードを1行も変えずに「監査」から始めた話
- 切り口: 新しいプロダクト仕様書（v2.0）を受け取った初日に、実装ではなくリポジトリ監査だけを行い、
  「現状マップ」と「要件⇔既存実装の対応表」を先に作った話。仕様書の用語と動いているコードの用語が
  別体系だったとき、どちらかに黙って寄せると事故になる。「仕様の語彙を実装の正として書かない」という
  表記ルールを対応表に敷いた工夫、既存の検証コマンドを無変更で回して「今の green」を固定してから
  次に進む段取りも材料になる。
- 想定読者: 稼働中プロダクトに大きめの仕様変更を入れる開発者 / AI エージェントに実装を任せるPM
- 状態: 未着手
- メモ: 「書き換えない勇気」「監査成果物が以降の全タスクの共通前提になる」が主題。
  社内ギャップの具体的な内訳・数値は公開前に要マスキング。
## AIクローラーに「自分が何者か」を伝える — llms.txt導入の話
- 切り口: robots.txtでAIクローラーを許可するだけでは不十分。llms.txtという新しい規格で「自分のサービスが何か」をAIに正しく伝える取り組み。SEOの次のフロンティアとしてのLLMO（Large Language Model Optimization）。
- 想定読者: SaaS運営者、マーケティング担当者
- 状態: 未着手
- メモ: llmstxt.org の仕様。siteConfigとPLANSから動的生成するので料金改定にも自動追従する実装。AIクローラー（GPTBot, ClaudeBot, PerplexityBot）の許可設定との組み合わせ。
- 公開区分: 公開可


  続編の材料（IMP-001、公開可）: 新旧語彙の「変換マップをあえて作らない」判断。
  `completed` と `VERIFIED` は厳密には同義でないのに、変換マップを先に出荷すると
  誤った同一視がコード上の事実として独り歩きする。マッピングは状態遷移の意味論を
  設計するタスク（IMP-015）まで遅らせ、代わりに型ガードのテストで legacy 値の混入を
  機械的に拒否させた。「正しくない橋は、架けないほうが安全」という話。


## 仕様書に色コードが書いてあっても、それを採用しないという「仕様準拠」もある
- 切り口: 新仕様書には Primary #155EEF などのトークン表が載っていたが、稼働中の全画面が
  参照する既存トークンの張り替えは実質リペイント。採ったのは「色値ではなく意味論だけを
  実装する」道 — HIGH=Warning / CRITICAL=Danger / RESOLVED=Success という対応表と、
  欠けていた8部品だけを既存デザインシステムの語彙で作った。仕様準拠とは字面の一致ではなく
  意図の充足、という話。ついでに、コメント内の「border-*/20」がブロックコメントを
  途中で閉じてビルドを壊した小咄も添えられる（コードに書く文章もコードである）。
- 想定読者: デザインシステムを運用している開発者 / 仕様書とコードの乖離に悩むPM
- 状態: 未着手
- メモ: IMP-010（2026-08-19）。DECISION_LOG 同日エントリが材料。内部数値なし・公開可。

## 連携先を1つ増やすのに、なぜ毎回テーブルとAPIと画面の3点セットが要るのか
- 切り口: 「色んな連携をログインだけで繋げるようにしたい」という一言から棚卸しを始めたら、
  6件中5件は既にワンクリックで繋がっていて、手作業が残っていたのは2件だけだった、という話。
  要望の言葉どおりに「全部作り直す」と大工事になるが、実測すると直すべき対象は1/3だった。
  もう一つの発見は、保存先が連携ごとに3系統（専用テーブル / 会計用テーブル / tenants の列）に
  分かれていて、連携を1つ足すたびに DBマイグレーション + APIルート + 画面の3点セットが
  必要になっていたこと。ここを「プロバイダ定義1ファイルで済む」形に変えた。
  抽象化を先に作らず、Slack という実際に必要な連携で1回動かしてから基盤として残した点も含めて書ける。
- 想定読者: SaaS を作っている開発者 / 外部連携の多いプロダクトのPM
- 状態: 未着手
- メモ: 「要望の言葉ではなく、実測した手作業の件数で優先順位を決める」が主題。
  数字: 連携6件中、ログインだけで繋がっていたのは5件（Square/freee/マネーフォワード/
  Googleカレンダー/Stripe）、手作業が残っていたのは Slack と LINE の2件。
  LINE の手順は7ステップでうち6つが LINE Developers Console 側の作業。

## 整備工場に「開発者コンソールでトークンを発行してください」と言ってはいけない
- 切り口: LINE連携の設定手順が、現場の人に渡せる手順書として成立していなかった話。
  「プロバイダーを作成」「Messaging APIチャネルを作成」「チャネルアクセストークン（長期）を発行」
  「Webhook URLを貼り戻して利用をON」「応答メッセージをOFF」——これは開発者向けの言葉で、
  日々クルマを触っている人に投げる作業ではない。結果として導入のたびにこちらが張り付いていた。
  SaaS の導入コストは、料金表ではなく設定画面の手順数に出る。
- 想定読者: 業界特化SaaSを売っている人 / 現場向けツールを作っている開発者
- 状態: 未着手
- メモ: 公開区分は公開可だが、**特定の加盟店名・導入社数・サポート工数の実数は書かない**
  （実数は未計測。書くなら計測してから）。
  **後日談として使える決着**: 「公式の申請が通れば全部消える」と分かった直後に、その申請の
  受付が停止中だと判明した。外部の許可が要る部分と要らない部分を分けて数え直したら、
  要らない部分だけで7手順のうち3つ（トークン発行・Webhook URLの貼り戻し・応答設定の確認）が
  消せた。**「許可待ち」は、手前の改善を止める理由にならない**という話にできる。
  数字: 7手順 → 入力2値。残った2つはLINE側にAPIが存在しないもの（Webhookの利用トグル・応答モード）。
## 「読み取れません」とすら出なかった——fail-soft のつもりが fail-silent だった話
- 切り口: 代表から「車検証の読み取りが出来ない」と一行の報告。追ってみると、エラーも出ず
  ボタンを押しても何も起きない状態だった。原因はOCRの精度ではなく、失敗の握りつぶし方。
  Vision API の呼び出し失敗を `console.error` だけで飲み込み、空のデータを「正常な結果」として
  返していたため、**原因の異なる3つ**——(1) AI自動入力の設定がOFF（または月次コスト上限に到達）、
  (2) APIキー未設定・レート制限・サーキットオープンといった基盤側の障害、(3) 単に画像が不鮮明——が
  すべて同じ「無反応」に潰れていた。設定を直せば済むのか、こちらの障害なのか、撮り直せばいいのか、
  現場からは一切区別がつかない。fail-soft（優しく劣化させる）のつもりの設計が、
  fail-silent（黙って死ぬ）になっていた。直したのは精度ではなく、**失敗が名乗るようにしたこと**。
  もう一つの発見: 自動コードレビューを回したら、その修正自体に「二次元コードが1つでも読めると
  例外を握りつぶす分岐が残り、新設したエラー表示が電子車検証では一度も発火しない」という
  実バグが見つかった。エラー処理を足すコードが、別の握りつぶしを作っていた。
- 想定読者: 自社ツールを内製している事業者、AIに実装を任せている人、
  「動かない」としか報告が上がってこない現場を持つ人。
- 状態: 未着手
- メモ: 公開可（設計判断のみ。テナント情報・個人情報・接続情報は含まない）。
  キーメッセージ:「"何も起きない"は、最悪のエラーメッセージである。」
  「fail-soft と fail-silent の差は、利用者が次に何をすればいいか分かるかどうか。」
  「エラー処理を足したコードにも、握りつぶしは残る。」
  ユーザーからの報告は「車検証の読み取りが出来ない」の一行だけだった、という導入が使える。
  実際にどの原因だったかは記事執筆時点で【要確認】（OPEN_QUESTIONS に起票済み）。

## 「メールアドレスを持っていないお客様が、実は一番多いかもしれない」— 本人確認の前提を疑った話
- 切り口: 顧客マイページのログインを、当たり前のように「メールアドレス＋電話番号下4桁でワンタイムコード」で作っていた。だが現場を通すとメールアドレスを持たない/教えないお客様がいる——スタッフがLINEのやり取りから起こした顧客は名前と電話だけ、登録フォームもメールは任意。その人たちは施工履歴を見る手段が**一つも無かった**。「施工履歴プラットフォーム」を名乗りながら、一部のお客様に対して履歴を見せる導線が存在しなかったということ。解いた筋は「新しい本人確認を足す」ではなく「**既に取れている本人性を使う**」。LINE連携は店が出したコードをお客様が送って成立するので、その時点で本人性は取れている。ならば連携できた瞬間に、単回使用・期限付きのログインリンクをそのトークにだけ流せばいい。弱点は「後でもう一度タップしたら切れている」こと。そこは、LINEに「マイページ」と送れば新しいリンクが返る導線を同時に用意した——LINE公式アカウントは受信への返信が無料なので、この再発行はコストがかからない。
- 想定読者: BtoC向けSaaS/業務アプリで顧客ログインを設計する人、「メールアドレスは全員持っている」を前提に作ってしまった人、LINEを顧客接点にしている事業者。
- 状態: 未着手
- メモ: 公開区分=要確認（認証設計を含むため対外発信は代表確認の上で）。**トークンのハッシュ化レシピ・有効期限の実値・本番の設定値は載せない**。キーメッセージ:「本人確認を足す前に、もう取れている本人性が無いか探す。」「一番弱いお客様が入口ごと締め出されていないか、機能ではなく人で確認する。」「安全側（単回使用）にした分の不便は、無料で取り直せる導線で埋める。」

## 「“連携できたら送る”をどこに置くか」— 通知の起点は画面ではなく、事実
- 切り口: 顧客がLINE連携を済ませても、マイページ（施工履歴の閲覧口）のURLは自動では届いていなかった。連携の入口は3つある——スタッフが受信箱から手で紐づける／顧客が連携コードを送る／登録フォームを出す。素直にやるなら「コードを送ってきた人への返信にURLを足す」で終わるが、それだと残り2経路の顧客には何も届かない。直すべきは画面ごとの導線ではなく「連携が成立した」という**事実**の側で、3経路が既に合流している共通処理にだけ1つ足せば全部が直った。もう一段の判断が課金で、LINE公式アカウントは**プッシュが従量課金・受信への返信は無料**。コード経路は直後に無料の返信を返しているので、そこへ同梱してプッシュを送らない分岐だけ入れた。ついでに「URLが組めないときは送らない」も入れた——ドメイン設定が抜けていると `/my?...` という壊れたリンクを顧客に送ってしまうので、送らない方がまだ信頼を損なわない。
- 想定読者: 通知・オンボーディング導線を設計する人、SaaSで「複数の入口から同じ状態に到達する」機能を作っている人、LINE公式アカウントの運用コストを気にする事業者。
- 状態: 未着手
- メモ: 公開可（設計判断のみ。顧客データ・実テナント・実URLは載せない）。キーメッセージ:「症状が出た経路ではなく、事実が成立する場所を直す。」「無料で届けられる経路があるなら、わざわざ有料で送らない。」「送れない条件では黙って送らない——壊れたリンクは無通知より悪い。」

## 「証明書が発行できない」——直そうとしたPRが、そもそも本番に届かなかった話
- 切り口: 現場から「証明書が発行できない」と1枚のスクショが届く。エラーは英語で
  "Could not find the 'damage_map_json' column"。コードは正しく、マイグレーションのファイルも
  リポジトリに存在する。にもかかわらず本番のDBにその列は無かった。適用履歴には「適用済み」と
  記録されているのに、中身だけが実行されていなかった——履歴が嘘をついていた。
  しかも発行処理はこの列を常に送るため、傷マップ機能を使っていない店舗の発行まで巻き添えで止まる。
  ここで「その1列を手で足して終わり」にせず全マイグレーションを本番と突き合わせたら、
  同じ未反映があと3ファイル見つかった。**本題はここから。** 修復マイグレーションを1本足したPRを作り、
  自動コードレビューにかけたら「このPRはマージしても障害を直さない」と返ってきた。
  修復を本番へ運ぶ自動適用の仕組み自体が13日前から赤で止まっており、マージしても何も適用されない。
  しかも「赤い」ことはリポジトリの未解決メモに自分たちで書いてあった。
  赤い間の変更は手作業で本番に直接当てられ、それがさらに履歴のズレを増やし、
  次のマージをもっと確実に止める——という自己増殖ループになっていた。
  直したのは、欠落分の冪等な再適用、履歴とリポジトリの整合、そして詰まりの解消。
- 想定読者: 小さなチームで本番DBを運用している開発者・事業者、AIに実装を任せている人、
  「テストは通るのに本番だけ壊れる」に心当たりのある人。
- 状態: 未着手
- メモ: 公開可（技術的失敗と対策のみ。テナント名・個人情報・接続情報・プロジェクトIDは書かない）。
  キーメッセージ:「報告された症状は入口であって、範囲ではない。」
  「直す前に、直したものが届く経路が生きているかを確かめる。」
  「“未解決”と書いて放置したメモは、次の障害の予告になる。」
  「壊れた通知は、壊れた仕組みより高くつく——13日気づかなかったのは仕組みではなく可視性の問題。」
  同種のドリフトが2026-07-31にも起き（そのときは直しただけで仕組みを潰さなかった）、
  ワークフロー失敗の通知も2026-07-21に「要検討」と書かれたまま2回スルーされている。
  1回目と2回目の対応の差、そして「書いたのに動かなかったメモ」が記事の骨格になる。

## 「昨日入れた“賢い最適化”を、翌日に現場感で撤回した話」— 品目選択を“隠す”から“純POSレジ型”へ戻した判断
- 切り口: 予約・会計の品目選択で「品目が多いと開いた瞬間に縦一列でどっと出て選びにくい」問題に、前日は"賢い"策を入れた——検索やカテゴリで絞るまで一覧を隠す段階表示。だが翌日、現場出身の代表から「隠さないでほしい。レジみたいに、常にカテゴリと品目を全部出して、タップで選ばせて」と要望。実店舗のレジは"まず全部見えて、押して選ぶ"のが速い。1アクション増やして隠す最適化は、店員に「品目が無い？」と誤認させる導線ですらあった。そこで隠すロジックを丸ごと撤去し、縦積みリストを**マス目グリッド**にしてPOSと操作感を統一。真因は「隠すこと」ではなく「縦積みで見にくいこと」で、そこを直したら差分は削除中心（追加53行・削除132行）で複雑性はむしろ下がった。
- 想定読者: 現場向けSaaS/業務アプリのUXを設計する人、「良かれと思った最適化」が現場で外れた経験のある作り手、AIに実装を任せている事業者。
- 状態: 未着手
- メモ: 公開可（UI/UX判断の学びのみ。機密・個人情報・本番データなし）。キーメッセージ:「推測ベースの"賢い最適化"より、現場の一次知見。」「隠す前に、そもそもの見にくさ（縦積み）を直せたか問う。」「最適化の撤回は敗北ではなく、コードが減って設計が良くなる方向。」

## 「“技術が、資産になる。”を、スローガンから送金まで実装した」— 施工記録が後年に収益として職人へ還り続ける仕組み
- 切り口: 中古車の履歴レポートが売れたとき、その売上を「その車に施工記録を残した店」へ**記録件数に比例して還元**する仕組みを、可視化（台帳）だけでなく**実際の送金（Stripe Connect）と返金時の巻き戻し**まで作り込んだ話。狙いは「今日残した1件の記録が、3年後に誰かが履歴を見るたび収益になる」——職人の技術が減価する労働ではなく積み上がる資産になる、という価値提案を“言葉”で終わらせないこと。設計の肝は3つ: (1) **開示した範囲＝還元される範囲を必ず一致**させる（買い手に見せていない店には還元しない／段階式で「直近Nヶ月だけ」買っても、その窓の記録を残した店にだけ還る）、(2) **還元率が確定するまで1円も動かない人手承認ゲート**（不可逆な送金の急所を守る）、(3) 開示0件のレポートは**そもそも売らない**（空の履歴に課金しない）。金銭移動なので、自動コードレビューを何ラウンドも回して「誤って送金を取り消す」「送金済みの資金が台帳から迷子になる」「障害が起きても監視が“正常”に見える」といった型のバグを潰してからマージした。
- 想定読者: 自動車の整備/鈑金/コーティングの職人・工場、業界のデータ活用に関心のある人、「AIで現場の価値をどう資産化するか」を考える事業者。
- 状態: 未着手
- メモ: 公開区分=要確認（対外発信は代表確認の上で「記録が収益として施工店に還る仕組みを送金まで実装した」枠に限定）。**還元率の具体数値（未確定・承認前）・本番プロジェクトID・テナント/実データ・価格の対外確定値は載せない**（数値そのものは機密のため本ファイルにも転記しない — CLAUDE.md 準拠）。開発運用の学び（金銭移動は不可逆＝急所なので、自動レビューを回して“誤reverse・資金の取り残し・無音障害”の型を潰してから出す／実バグは潰し、共有基盤の構造改善は別issueに切り出して際限のないレビューループを止める）は公開可。

## 「“完全自動”と書かなかった理由」— 自動化SaaSのLPで、あえて『確定は人』を売りにした話
- 切り口: LINE対応・予約作成・アフターフォロー・証明書発行・帳票作成をAIで自動化したSaaSのHPを更新するとき、普通は「全部自動！」と打ち出したくなる。だが自動車業界の信頼低下・不正が出発点のプロダクトで、金額確定・本人確認・証明書発行まで機械任せにするのは筋が悪い。そこで訴求の軸を「AIが下ごしらえ、確定は人。」に置き、"人が最終確認する領域（壁3）"を弱みではなく信頼の担保＝売りとして前面に出した。繰り返しの入力はAIが肩代わりし、責任の伴う操作だけ人が握る——という線引きをそのままコピーにした。もう一つの裏話: 載せる前に「その機能、本当に実装されてる？」を一次情報（社内の自動化ガイドと実コード）で全部照合してから書いた。マーケコピーが実装を追い越さないための規律。
- 想定読者: 自動化/AI SaaS を作る事業者、LP・訴求を設計する人、「AIで全部やります」の過剰訴求に違和感がある人。
- 状態: 未着手
- メモ: 公開可（訴求設計・「自動化の線引きを売りにする」考え方のみ）。プラン内部設定・テナントID・本番データは載せない。キーメッセージ:「自動化の価値は"全部やる"ことではなく"人が確認すべき所を残す"こと」「掲載前に実装と照合＝コピーが実装を追い越さない」。

## 「“配布を楽にする”ために、あえて実装を増やした」— Custom AppsをやめてApp Store一般公開に振った判断
- 切り口: iPhoneをカードリーダーにする Tap to Pay を載せたモバイルアプリを、どう配るか。当初は招待制の Custom Apps（Apple Business Manager 経由）にして、Appleの「アプリ内サインアップ必須」要件を"免除"で回避する設計だった。だが招待制は加盟のたびに本部が手動配布する運用になり、属人化・配布ミス対応が重い。そこで方針を反転し、App Store一般公開に切替。免除していた「アプリ内で新規登録→利用開始が完結」「アプリ内でアカウント削除」を作り込むコスト（数日）を受け入れた。ポイントは"手間を減らすために実装を増やす"という一見逆の判断——恒久的な運用コストと属人リスクを、一度の実装で消しにいった。もう一つの学び: 「未実装リスト」を鵜呑みにせず実コードを読んだら、TTP対応の大半は既に完成していて、本当の障壁は配布チャネルの選択という"設計判断"だった。手を動かす前に一次情報（コード）を見ると、やるべき作業の地図が変わる。
- 想定読者: 個人〜小規模で自社アプリを出す事業者、モバイル配布戦略で悩む人、AIと開発を回して「作業リスト」が増えがちな人。
- 状態: 未着手
- メモ: 公開可（戦略・設計判断の知見のみ）。Apple Team ID / ASC App ID / Bundle ID / テナントID等の識別子、審査個別のやり取りは載せない。「配布の楽さ＝実装の少なさ、ではない」「免除で逃げるか、要件を満たして正面から出るかは運用コストで決める」。

## 「AIエージェントに“溜まったPR13件”を片付けさせたら、rebaseではなく“作り直し”を選んだ」
- 切り口: オープンPRが13件滞留。素朴には全部rebaseしてマージだが、mainから最大56コミット分岐した機能PRはrebaseでコンフリクト地獄＆履歴断絶で健全にマージできない。とった判断は「ブランチ履歴」と「コード差分」を分離し、価値ある差分だけを現mainへ最小再適用して新PRを立て、コードレビューとCI緑を通してからマージする、という運用。依存Bump/docs/機能/WIPで扱いを4分割し、実送金や大型UI同期は無理にマージせず保留。さらに再適用の過程でコードレビューが webhook 二重発火の冪等性バグを発見し、compare-and-swap で修正——「作り直し」がバグ発見の機会にもなった。AIと現場で高速に開発を回すと生まれる“PRの滞留”を、機械的マージではなく健全化の観点で片付けた話。
- 想定読者: 小さなチームでGitHub運用する開発者、AIコーディングでPRが増えがちな人、レビュー/CI運用を設計する人。
- 状態: 未着手
- メモ: 内訳=マージ4・クローズ8・再作成3・保留2。「rebaseより差分再適用が速い場合がある」「PR整理は依存/docs/機能/WIPで扱いを分ける」。具体的なテナント・本番プロジェクトIDは載せない（運用知見のみ公開可）。

## 「インフラを疑って半日、真因は `Number("")===0` の1行だった」― 帳票が全部消えたデバッグ記
- 切り口: 本番で帳票一覧が突然0件に。DBには28件ある、権限(service_role)も接続先も環境変数も正しい——なのにアプリ経由だけ0。RLS・接続プールの過渡的不具合・環境変数ミスと順に疑い、リトライで“温める”対症策まで入れたが直らなかった。決め手はサーバのAPIログで「実際に飛んでいるクエリ」を1行見たこと。そこには誰も指定していない `total>=0 AND total<=0`(＝total=0) という金額フィルタが常時付いていた。原因は「金額の検索欄が未入力のとき `Number("")` が 0 になる」JavaScriptの挙動。空を『フィルタ無し』ではなく『0で絞り込み』と解釈し、金額>0の全帳票を弾いていた。教訓は3つ:「インフラを疑う前に“実際に発行されたクエリ”を見る」「症状(0件)に対症療法(リトライ)を積むと真因から遠ざかる」「`Number(空文字)===0` は定番の罠——空判定を先に書く」。AIと高速に実装を回すからこそ、症状ではなく一次ソース(ログ・実クエリ)で真因を掴む規律が要る、という話に接続できる。
- 想定読者: Web/SaaS を本番運用するエンジニア、小さなチームで自分でデバッグする開発者、AIコーディングを実務で使う人。
- 状態: 未着手
- メモ: `parseAmountParam`(空→null)、対症策(多重リトライ/診断)を撤去して単一クエリに戻した判断、回帰テスト追加。本番プロジェクトID・テナントID・実データ件数は載せない（技術的教訓のみ公開可）。

## 「セキュリティ対策のはずが、本番だけ壊す」― DB関数の search_path='' が両刃だった話
- 切り口: Supabase/Postgres の SECURITY DEFINER 関数には `SET search_path=''` を付けるのがセキュリティ定石（search_path ハイジャック対策）。だが `search_path=''` は「関数本体のテーブル参照を全部スキーマ完全修飾している」ことが大前提で、本体が非修飾のままだと逆に本番で必ず `relation does not exist` を吐いて壊れる。実際、対策を一括で付けた際に本体の修飾を直し忘れた関数が数か月放置され、証明書統計や保険会社ポータルが静かに失敗していた。しかも直すと今度は「enum に無い値を参照していた」二次バグが露出。教訓は「セキュリティ設定はそれ単体では完成せず、前提条件（完全修飾）とセットで初めて安全」「一括対策こそ取りこぼしが起きやすく、後追い検証が要る」。AIと現場でコードを高速に回すからこそ、こうした“設定の前提”を検証する仕組みが要るという話にもつなげられる。
- 想定読者: Supabase/Postgres を本番運用するエンジニア、SECURITY DEFINER を使う開発者、小さなチームでDBを触る人。
- 状態: 未着手
- メモ: 対象4関数、二重バグ（バレ参照→enum 'expired'）、既存の確立パターン（status::text 比較）に寄せて最小差分で修正。具体的な本番プロジェクトIDや統計値は載せない（技術的教訓のみ公開可）。


## 「営業資料が"勝手に最新"になる仕組み」― 代理店が古い資料で商談しないために
- 切り口: 代理店（パートナー）が配る商品資料は、機能追加や料金改定のたびに古くなる。本部が毎回
  差し替える運用は必ず漏れる。Ledra は料金・機能一覧・比較表を「1箇所のデータ」から持ち、
  資料PDFはダウンロードのたびにその場で生成する設計にしているので、機能が増減しても代理店は
  常に最新版を出力できる（本部の差し替え作業ゼロ）。既にマーケ向けにあった自動生成資料を
  代理店ポータルにも“配線し直すだけ”で実現した、既存資産の再利用という判断も。ただし契約書
  テンプレのように機能と連動しない定型文書は自動化の対象外、という線引きも正直に。
- 想定読者: 代理店・FC 型で営業資料を配布している事業者、SaaSの資料運用に悩む人。
- 状態: 未着手
- メモ: 「静的ファイルを配る」から「データから毎回生成する」への発想転換が肝。数字・固有名詞は
  公開前に要確認。機密・個人情報は含めない。

## 「『レドラ』って呼んで起動」は作らなかった ― OS制約を"既にあるもの"で回避した話
- 切り口: 現場から「モバイルで『レドラ』と声で呼んで起動できる?」という要望。理想は"アプリを閉じていても声だけで起動"だが、常時ホットワードは OS のアシスタント専用で third-party アプリには開放されていない（バッテリー・プライバシー）。この制約を正直に説明した上で、新規実装ではなく既にある URL スキーム（`ledra://`）＋ iOS ショートカット/Android ルーティンに「レドラ」を登録するだけで"声で起動"を今日から成立させた。「作らないのが最良のコード」を地で行った意思決定。実機検証できないネイティブ音声認識は無理に載せず、ロードマップとして分離した誠実さも。
- 想定読者: 現場要望を"言われたまま"作らず、制約と既存資産を突き合わせて最小で解きたいプロダクト開発者。モバイルの音声起動を検討している人。
- 状態: 未着手
- メモ: 材料は `apps/mobile/docs/VOICE_LAUNCH.md` と DECISION_LOG 2026-07-28。機密情報なし。

## 「技術が、資産になる。」を、言葉から仕組みに変えた話 ― 記録が後年に収益を生む設計
- 切り口: 「今日残した施工記録1件が、3年後に誰かがその車の履歴を見るたびに収益になる」を、
  スローガンではなく実装（レポート売上を記録元の施工店へ按分し蓄積する台帳）として作った過程。
  ポイントは (1) 記録を作った人に価値が返る循環＝情報非対称の解消がそのままマネタイズになる、
  (2) 不可逆な送金を先に走らせず「見える収益」を先に届ける（蓄積台帳→後精算）という順番の設計判断、
  (3) 按分は"残した記録の件数"に比例させ「残した分だけ資産になる」直感と一致させたこと。
- 想定読者: 現場の記録・データが「作った人に返らない」構造に課題を感じている事業者/開発者。
  ストック型の価値提案を実装で裏づけたい人。
- 状態: 未着手
- メモ: 還元率などの具体数値・未送金である実装段階は公開前に代表確認（機密/要確認）。数字を出さずに
  「件数比例で還元」「まず可視化、送金は後精算」という設計思想の枠で書けば発信可能。

## 「90画面を"聞いて開く"」― 管理画面の肥大化に、AIナビと横断検索で挑んだ話
- 切り口: 機能が増えてサイドバーが9グループ・数十項目に膨張し「目的の画面が探せない」状態を、
  加算的・既存再利用の方針で解いた。常時表示をコア8機能に絞り、残りは「AIに聞く」チャット
  （自由文→画面遷移）と横断検索（名前・番号で顧客/車両/証明書/請求書へ）で到達。AI が返す遷移先は
  必ず既知カタログに実在するか照合してから使う（ハルシネーション/オープンリダイレクト防止）という
  トラスト境界の作り方も。AI 不通時は静的フィルタにフォールバックしてナビを止めない設計。
- 想定読者: 機能過多の業務SaaSを運用/開発している人、AIを"魔法"でなく安全な導線として組み込みたい人。
- 状態: 未着手
- メモ: 「AIの出力をそのまま遷移に使わず、実在チェックを噛ませる」「落ちても静的フォールバックで
  止めない」の2点が実務的な肝。数字・固有名詞は公開前に要確認。

## 「リポジトリ全体が赤かった」― 直せない脆弱性警告と、CIゲートの落としどころ
- 切り口: 依存の脆弱性(brace-expansion)が「単一バージョンでしか直らず、直すと別のツールが壊れる」
  という手詰まりで CI 必須チェックが全PR・mainまで赤に。ゲートを緩めず（highはブロックのまま）、
  依存の版を揃え直して両立させ、テスト/型/lintで回帰無しを実測してから通した判断の記録。
- 想定読者: 小さなチームで CI とセキュリティ監査ゲートを回している開発者。
- 状態: 未着手
- メモ: 「監査レベルを下げて黙らせる」誘惑に対し、厳しさを保ったまま解いた過程。技術詳細が濃いので
  一般向けには噛み砕きが必要。

## 「整備記録簿の"電子化OK"通知に、実装で答え合わせをした話」― 法規制と自社機能のマッピング
- 切り口: 国が令和7年に「点検整備記録簿・特定整備記録簿・指定整備記録簿を電子で作成/保存/交付してよい」
  という取扱いを周知した。紙が当たり前だった整備記録が電子化できるようになった、その要件（直ちに表示できる・
  検索できる・改ざん防止・作成/更新/消去と作業者を自動記録・使用者への電子交付は事前承諾が要る 等）を、
  自社の既存機能と一つずつ突き合わせて「どこまで満たしているか」を可視化したマッピング作業の記録。
  「制度が電子化を許した瞬間に、プロダクトが胸を張って準拠を示せるか」は、後追いでなく先回りの資料化で決まる、
  という運用の話にできる。
- 想定読者: 整備・車体整備の事業者、業務のペーパーレス化・法対応を検討している人、規制対応をプロダクトに
  落とし込む開発者/PM。
- 状態: 未着手
- メモ: 公開時は「準拠済み」と断定せず、対応済みの範囲に限って positive framing。未対応ギャップ（資格ロール・
  交付承諾撤回・指定整備記録簿様式 等）は対外発信に含めない。数字・固有名詞・準拠可否は公開前に代表要確認。
- 公開区分: 要確認

## 「真正性チェックはデフォルト無害に設計する」― 誤検知で現場を止めないAI真正性の作り方
- 切り口: 写真の位置情報や電子署名で「本物かどうか」を裏で確認する仕組みを足すとき、検出したら強く警告するほど良いと思いがち。だが出張作業がメインの現場では「店舗から離れた場所で撮影」が普通で、そのまま警告すると毎回誤報になる。そこで (1) 既定設定では余計なフラグを一切増やさない「デフォルト無害」、(2) 改変が確定的なものだけを"要対応"、位置ズレ等は"要確認"止まりで作業を止めない、(3) 生の位置座標は保存せず照合結果だけ残す、(4) 画像を見ても判断できない項目（位置・署名）はわざわざAIに再確認させず無駄なコストを出さない、という設計原則で「真正性は上げるが現場・プライバシー・コストは壊さない」を両立した話。出張現場は端末GPSを作業記録に紐づけて"正当な場所"として扱う。
- 想定読者: 現場業務にAI検証を足すプロダクト開発者・PM、誤検知の運用コストに悩むチーム。
- 状態: 未着手
- メモ: 競合の実名は書かない。一般化した設計原則（デフォルト無害／非決定的優先／生データ非保存／無駄な推論を呼ばない）に留める。裏付けは RELEASE_LOG / DECISION_LOG 2026-07-27 フェーズ2エントリ。公開区分＝公開可（一般的な設計知見）。

## 位置情報は「使うけど残さない」― プライバシーを守りながら写真の撮影場所を検証する
- 切り口: 施工写真には撮影場所（GPS）が埋まっていることがあり、顧客の自宅位置になり得るため保存は避けたい。一方で「その写真が本当に作業場所で撮られたか」の確認には使いたい。そこで、アップロード処理の最中だけメモリ上で座標を店舗/作業場所と突き合わせ、"近い/遠い"の判定と大まかな距離帯だけを残して**生の座標は即破棄**する設計にした話。出張作業場所の座標を持つ場合もスタッフ運用限定で顧客・保険会社には一切出さない。「活用と非保存の両立」の具体的な実装判断。
- 想定読者: 位置情報や個人データを扱うプロダクトの開発者・PM、プライバシー配慮の実装例を探しているチーム。
- 状態: 未着手
- メモ: 個人情報・具体的な顧客データは書かない。教訓は「照合はメモリ内で、保存は結果のみ」「距離は帯に丸めて精密な位置を残さない」「基準座標は最小権限」。公開区分＝要確認（プライバシー方針の外部公開文面は代表確認後）。


## 「写真を撮るだけ」を新規開発せずに実装した ― 競合の目玉機能を"既存資産の接続"で最短で作る
- 切り口: 競合が「写真を撮るだけで作業時刻・工程が自動で残る」を売りにしていた。同じ体験をゼロから作るのではなく、自社に既にあった部品（写真の撮影時刻を読む処理・画像から情報を取り出すAI・車検証の読み取り・顧客/車両/担当の自動ひも付け）を"繋ぐ"だけで、手入力を大きく減らす下書き（施工日・作業時間の推定）を最小の変更で足した話。派手な新機能より「既にあるものを繋ぐ」ほうが速くて壊れにくい、という開発判断。撮影時刻を扱うときの落とし穴（タイムゾーンで日付が1日ずれる／壊れた時計や別日の写真を鵜呑みにしない＝AIは事実を作らない）も具体例として書ける。
- 想定読者: 自社プロダクトに機能を後付けする開発者・PM、競合に機能で追いつきたい小規模チーム。
- 状態: 未着手
- メモ: 競合の実名・優劣比較は書かない（公開区分＝要確認）。「既存資産の接続」「撮影時刻の扱いの注意点」「提案止まりで確定は人」という一般化した設計知見に留める。裏付けは RELEASE_LOG / DECISION_LOG 2026-07-27 フェーズ1エントリ。

## 「CIが赤いのにログは警告だらけで真因が見えない」― 末尾ログでなく手元での並列再現で犯人を特定した話
- 切り口: CIの「Lint/型/テスト」ジョブが失敗したがログ末尾は既存コードのeslint警告(0 errors)で埋まり、真の失敗が隠れていた。plainなeslintは警告では落ちない＝犯人でないと切り分け、CIと同条件（同じNode・4コマンド並列）を手元で再現して真因＝別PRとのマイグレーション・バージョン衝突を一発で特定・修正した話。「エラーメッセージの見た目に釣られず、失敗コマンドを実際に走らせて分離する」という地味だが効くデバッグ習慣。
- 想定読者: CIの失敗切り分けに時間を溶かしている開発者、小規模チームのCI運用者。
- 状態: 未着手
- メモ: 具体的な社内秘・顧客情報は書かない。教訓は「並列ジョブの失敗は末尾ログでなく各コマンドを個別に再現して分離」「同日マイグレーションもHHMMSSまで一意化」。公開区分＝公開可（一般的な技術知見）。

## 「透明化」が業界の合言葉になった年 ― 自動車修理DXに"改ざんできない証跡"が求められる理由
- 切り口: 国交省が2025年に自動車整備業を労務費の転嫁率ワースト1位と公表し、「車体整備の消費者に対する透明性確保に向けたガイドライン」への対応が現場の共通課題に。ここに「作業を写真で証明し、改ざんできない形で残す」プロダクトが相次いで登場している、という業界地図を描く。Ledra の立ち位置は"証明を作る"だけでなく"第三者（保険会社）が独立検証できるところまで担う"点、という切り口で。
- 想定読者: 自動車整備・鈑金塗装の経営者、業界のDX動向を追う人。
- 状態: 未着手
- メモ: 競合の実名・優劣比較は書かない（公開区分＝要確認）。国交省の公表事実と「透明化＝改ざん不能な証跡」という一般論に留める。数値は一次ソース確認済みのもののみ使う。裏付け一次情報は DECISION_LOG 2026-07-27 エントリ参照。

## 「一機能を製品名にしていた」問題 ― SEOを実態に合わせ、AI検索にも拾わせるまで
- 切り口: 立ち上げ期に旗印にした「WEB施工証明書」という一語が、プロダクトが予約・請求・顧客管理・保険連携・
  AI自動化まで広がった今では逆に製品を狭く見せていた。差別化機能（改ざん検知付き施工証明書）は柱として残しつつ、
  位置づけを「自動車整備・コーティング店のAI業務管理SaaS」に統一。あわせて、Googleだけでなく生成AI検索
  （ChatGPT/Perplexity/Gemini）に「Ledraは何ができて誰向けか」を事実で拾わせるため、JSON-LDのfeatureList/audienceを
  厚くし、robots.txtでAIクローラーを名指しで歓迎した（GEO/AEO）。文言をsiteConfig一箇所に集約してブレを止めた設計も。
- 想定読者: 成長に伴い当初のキャッチコピーと実態がズレてきたSaaS/事業の作り手。AI検索時代のSEO(GEO)に関心のある人。
- 状態: 未着手
- メモ: before「WEB施工証明書SaaS」→ after「自動車整備・コーティング店のAI業務管理SaaS」。keywords 15語 / featureList 10項目 /
  AIクローラー7種を allow。効果（順位・AI引用率）は計測待ちで、記事化は数値が出てからが望ましい。

## 「1円のズレ」と「飛ばない完了通知」を潰す ― コア業務フローを5系統に割って横断バグ監査した話
- 切り口: 予約受付から会計終了までの一本道を「予約/工程/写真・OCR/AI自動化/会計・決済」の5系統に分け、
  各系統を並列で読み切り、候補を1件ずつ実コードで裏取りしてから直す、というやり方でバグ監査した。
  見つかったのは派手なクラッシュではなく、静かに効く実害——並列ブース枠で2件目の予約が必ず断られる、
  返金が上書きで累計を超えて記録される、会計の税端数が決済額と1円ズレる、完了通知が最終工程の設定次第で
  飛ばない、無防備なOCRエンドポイント。「絶対に壊れてはいけないコア」ほど、通しの整合が抜けるという話。
  直す/直さないの線引き（局所で安全に直せるものは直し、DB制約や製品判断を要するものは起票に回す）も。
- 想定読者: 自社の基幹フローの品質に責任を持つ開発者・PM。個別テストは通るのに通しで壊れる、を経験した人。
- 状態: 未着手
- メモ: 具体的な脆弱点の手口・顧客名・金額実データは書かない。「計算単位を揃える」「累計で判定する」
  「通知を状態から切り離す」といった一般化した教訓レベルにとどめる。

## 「裏側はAI、UIはSaaSのまま」でLedraをAIプロダクトっぽく見せた話 ― 根拠の無いところには数値を出さないという線引き
- 切り口: LedraはSaaSとして作ってきたが、裏側には既に90近いAIモジュールがある。これを「AIっぽいUX」に見せるため、
  ダッシュボードに自由入力の「聞く」窓口を1つ足した。ポイントは新しいAIロジックをほぼ書かず、まず無料の
  キーワードルーティングを試し、当たらない時だけ既存のAI回答エンジンに繋いだこと。もう一つは承認待ちのAI下書きに
  「なぜ」を出す時、証明書は本物の信頼度が取れたので出したが、請求書は自動作成か手動作成かを区別するデータが
  存在しなかったので、あえて何も表示しないと決めた話。「AIは事実を作ってはいけない」を機能の見た目より優先した。
- 想定読者: 自社プロダクトにAI機能を後付けで足している開発者・PM。「AIっぽく見せる」ことと「嘘をつかない」ことの
  両立に悩む人。
- 状態: 未着手
- メモ: 機密・個人情報は書かない。具体的な信頼度の数値例や顧客名は出さない。

## 壊れた監視 cron を「鳴らし続ける」のをやめる ― 設定ミスは skip、本物の障害だけ通知する
- 切り口: 残高監視 cron が、秘密鍵の些細な形式ミス（`0x` の付け忘れ）で510回超も「失敗」を鳴らし続けていた。
  修正は"リトライを足す"ことではなく「失敗の種類を分ける」こと ― 設定ミスは skip して失敗連鎖に積まず、本物の
  RPC 障害だけ通知する。ついでに直す場所を「呼び出し元」ではなく「共有関数」にして、監視とアンカリング署名の
  両方を一度に直した話。
- 想定読者: 監視/アラートを運用する開発者、cron・バッチの失敗設計に悩む個人〜小規模の運用者。
- 状態: 未着手
- メモ: 教訓は2つ ―（1）アラート疲れは「全部を失敗として鳴らす」設計から生まれる、種類を分ければ止まる。
  （2）バグは症状（呼び出し元）でなく根（共有関数）で直す。※ 秘密鍵の値・ウォレットアドレス・アンカリング基盤の
  具体は書かない（機密）。公開区分は「要確認」。

## エラーメッセージが「internal_error」としか出ない ― コードとメッセージを取り違えたバグを83ファイル分見つけた話
- 切り口: 画面のエラー表示に "internal_error" のような意味不明な文字列だけが出るバグを1画面分直そうとしたら、
  自作の統一エラーレスポンス関数（`{error: コード, message: 人間向け文}`）に対して、フロントの catch 節が
  `message` ではなく `error`（コード）を読んでいる箇所がリポジトリ全体で83ファイルも見つかった話。「1箇所直したら
  同じ関数を呼んでいる他の場所も grep する」を徹底したことで発見でき、今回は影響範囲を絞って17箇所だけ直し、
  残りは棚卸しリストとして残した、という地味だが誰もが踏みうる話。
- 想定読者: 自社プロダクトのエラーハンドリングを設計している開発者。
- 状態: 未着手
- メモ: 「バグ修正依頼が来たら、まず同じ関数の他の呼び出し元を全部見る」という開発習慣の効能の実例として使える。
  具体的な顧客名・案件名は出さない。

## 「放送と同時に記事を出す」ために予約投稿を自作した ― 5分おき cron ＋ 非公開を RLS で担保
- 切り口: 令和の虎の放送(土19:00)に記事を"同時に"出したいが、手動公開は放送時刻に人が張り付く前提で不確実。
  そこで CMS に予約投稿（`scheduled` ＋ 公開日時 ＋ 5分おき cron で自動昇格）を1本の cron で足した話。ポイントは
  「予約中に絶対に漏らさない」を機能ではなく DB の公開ポリシー(RLS: published のみ読める)で構造的に担保したこと。
  記事は放送5分前(18:55)に自動公開されるよう仕込んだ。
- 想定読者: 個人〜小規模で自社プロダクトを回す開発者、CMS/publishing を自作している人。
- 状態: 未着手
- メモ: おまけの"war story"として、予約用マイグレーションが本番で連続失敗→真因は別々の PR が同じ version 番号を
  使った衝突＋本番への out-of-band 適用ドリフト、を「本番記録に合わせて改名(forward)」で解いた話も書ける。
  「マイグレーションの唯一の真実は本番スキーマ」という教訓。機密・個人情報は書かない。保険会社提携等の非事実も書かない。

## 露出イベントのプレスは「秒」で刈り取る ― 番組出演のプレスリリースを"放送同時×週明け本命"で設計した話
- 切り口: YouTube公開型の番組（令和の虎）出演に合わせ、プレスリリースをいつ出すかを設計した話。土曜19:00の
  放送に土曜夜の一斉配信をぶつけると記者が動かず本命が死ぬ。かわりに「放送同時は自社チャネル(検索スパイク受け)
  だけ」「記者が動く週明け火曜に PR TIMES 本命」という段階配信にした。さらに"配信タイミングより、19:00時点で
  LP・お知らせが更新済みで問い合わせ導線が生きていることの方が効く"という急所の話。露出の広告価値を漏らさない
  ための実務設計として、スタートアップ・広報担当に刺さる。
- 想定読者: スタートアップの広報/マーケ担当、メディア露出を控える経営者、PR設計を学びたい人。
- 状態: 未着手
- メモ: DECISION_LOG.md 2026-07-21「令和の虎のプレスタイミング」が一次材料。出演の事実・結果・損保実名・自社の
  具体数字は放送/許諾/確定前は載せない（公開区分=要確認）。書けるのは"タイミング設計の考え方"まで。
## 露出の広告価値を「1円も漏らさない」ための計測 ― バニティリンク＋first-touch UTM、そして AI レビューが本番破綻を止めた話
- 切り口: テレビ/YouTube 露出は"見た人がその足で問い合わせるか"が価値。だが `/tora` のような覚えやすいリンクで
  着地させても、そこから問い合わせフォームへ進む間に流入元(utm)が消え、「放送経由の問い合わせ」が計測から漏れる。
  これをサーバ側(proxy cookie)で着地時に確定させ、ハイドレート前にタップしても取りこぼさない実装にした。加えて、
  実装中に AI コードレビュー(Codex)が「その置き方だと本番ビルドが壊れる／モバイルで取りこぼす」を"デプロイ前"に
  指摘し、設計を差し替えた ― HOLY の「AIと現場知見を融合」を開発プロセス自体で体現した具体例。
- 想定読者: スタートアップの広報/マーケ/エンジニア、計測設計を軽視して露出を"垂れ流している"事業者、AI 活用開発に関心のある人。
- 状態: 未着手
- メモ: DECISION_LOG.md / RELEASE_LOG.md 2026-07-21「first-touch UTM 帰属」が一次材料。具体的な問い合わせ数・
  CV 率は載せない（公開区分=要確認）。書けるのは"計測設計の考え方"と"AI レビューが事前に破綻を止めた"開発プロセスまで。
## LINEの「初手で連携をお願いします」がお客様を逃す ― オンボーディングの引き算

- 切り口: LINE公式アカウントは店から先に送れず、必ずお客様発でやり取りが始まる。だからこそ最初の数通が勝負で、
  ここで「登録フォームに記入を」「連携コードを送信を」と要求すると、価値が伝わる前に離脱してしまう。Ledra では
  連携のお願いを「2通目」から「4通目」へ後ろ倒しし、まず用件に答えてから案内する形に直した。機能を足すのではなく
  「いつ言わないか」を設計するという、現場目線のUXの引き算を語れる。
- 想定読者: 店舗でLINE公式アカウントを運用している事業者、オンボーディング設計に悩む人。
- 状態: 未着手
- メモ: 変更は送信タイミングの既定値のみ（env で店ごとに調整可能）。数字(2→4)の最適値は実運用の離脱率/連携率で要検証。

## 「同業者に電話で頼む」をやめる ― 加盟店同士が手数料0で仕事を回す仕組み

- 切り口: 出張整備・専門施工の現場では、遠方案件や専門外の作業を「知り合いの店に電話で頼む」文化が根強い。
  Ledra では加盟店同士の受発注をアプリ内で完結でき、既存取引先への「指名」依頼はプラットフォーム手数料を
  取らず、請求書払い（掛売）で顧客ごとの締め日にまとめて請求書を自動生成・確認後に送付する形にした。
  「見知らぬ相手を仲介するマッチング」と「既に信頼関係のある取引先との継続取引」を分け、後者からは手数料を
  取らないという設計思想（大手の中抜き構造への問題意識）を、実装の分岐（公開案件=手数料あり / 指名=0）と
  合わせて語れる。将来は空き枠の確認・押さえまでアプリで完結し「作業者が電話対応に追われない」現場を目指す。
- 想定読者: 整備・鈑金・コーティング等の工場経営者、多店舗連携を模索する事業者、B2B SaaS を作る開発者。
- 状態: 未着手
- メモ: DECISION_LOG.md 2026-07-20「指名BtoB請求」エントリが一次材料。手数料率の具体や料金設計、特定店舗の
  取引額・個人情報は載せない（公開区分=要確認）。Phase 2（空き確認＋枠押さえ）は構想段階として触れる程度に。

## 「電話で枠を押さえる」をやめる ― 二重押さえを防ぐ仮押さえの作り方
- 切り口: 取引先に作業を頼むとき「今週空いてる？」の電話が現場を止める。Ledra では取引先の空きをアプリで見て
  枠を仮押さえ→相手が受注承認で本予約、という流れにした。技術的な肝は「二店が同じ最後の1枠を同時に押さえる」
  競合を防ぐこと。素朴な『空き数を数える→入れる』は非原子で競合窓があるため、DB の advisory lock で
  (店舗,日,枠) ごとに直列化し、占有（既存予約＋有効な仮押さえ）を数えてから入れる、を1関数に閉じ込めた
  （社内の会計番号採番で使っていた手法の再利用）。「新機能はまず既存の手法を探して流用する」設計の実例。
- 想定読者: 予約・在庫など"最後の1個"競合を扱うプロダクトの開発者、B2B 連携を作るチーム。
- 状態: 未着手
- メモ: DECISION_LOG.md 2026-07-20「他店の空き確認＋枠押さえ(Phase 2)」が一次材料。特定店舗の空き・取引額・
  個人情報は載せない。プライバシー(誰に空きを見せるか)は「取引先=許可制」に留めた点も語れる。

## 「終日お預かり」をどう予約に落とすか ― 既存の暗黙ルールに逆らわない機能追加
- 切り口: コーティングやPPFのように「朝預けて夕方引き取り」の1日仕事を、時間枠だけの予約システムに
  後付けで載せる話。素直に 00:00〜23:59 の枠として保存する手もあるが、Ledra は元々「時刻が空＝終日」を
  カレンダー同期や店頭表示で暗黙に使っていた。新機能はその既存ルールに揃え、`all_day` フラグ1つで
  「終日という意図」を明示しつつ、競合チェック（ダブルブッキング防止）だけは"当日を丸ごと押さえる"よう
  DB関数を1箇所直す、という最小差分にした。「新機能ほど既存の慣習を調べてから足す」という現場的な設計判断の例。
- 想定読者: 予約・スケジュール系のプロダクトを作る開発者、機能追加で技術的負債を増やしたくないチーム。
- 状態: 未着手
- メモ: DECISION_LOG.md 2026-07-20「予約に終日を追加」が一次材料。特定テナントの数値・個人情報は含めない。

## 「返信は返したのに処理が消える」― サーバーレスの fire-and-forget 落とし穴

- 切り口: LINEの自動応答のように「まず200を即返し、重い処理は裏で続ける」構成は、
  サーバーレス環境だとレスポンス送出後に裏処理が無言で打ち切られることがある。Ledraでは
  予約確定時の会計提案・カレンダー同期が撃ちっぱなし（未await）で走っており、
  Next.jsの `after()` に載せた後も「外側が先に終わると中の未完了処理が捨てられる」問題が残っていた。
  await して完走を保証する形に直した実例。「裏で流しておけば大丈夫」がなぜ危ういかを、
  テストで再現（撃ちっぱなしだと落ちる回帰テスト）まで含めて説明できる。
- 想定読者: 業務システム／SaaSを作る開発者、サーバーレス移行を検討中のチーム。
- 状態: 未着手
- メモ: DECISION_LOG.md 2026-07-18「LINE予約枠確定の背景処理は…after()内でawait完走」エントリが
  一次材料。特定テナントの数値・個人情報は含めない。技術ネタなので事業機密には触れない。

## 「撮り忘れ」をゼロにする ― 現場を止めない写真ガイドの作り方

- 切り口: 整備・施工の現場で起きる「必要な写真の撮り忘れ」を、長いフォームや強制停止で
  解決しようとすると現場に嫌われる。Ledraは工程ごとに「撮る写真・確認する項目」を
  ベテランが宣言し、作業者にはガイドとして提示・タップで確認、未確認でも一度警告するだけで
  必ず進める設計にした。「強制停止は最小限」という思想を、実装レベル（サーバ側ハードゲートを
  あえて採らずUI側ソフト警告にした判断）まで落として説明できる。
- 想定読者: 整備・鈑金・コーティング店の経営者/現場責任者、業務システムを作る開発者。
- 状態: 未着手
- メモ: DECISION_LOG.md 2026-07-17 エントリ（強制ゲートを捨ててソフト警告にした理由・
  過去の「進行不能バグ」回避）が一次材料。特定テナントの数値・個人情報は含めない。

## 整備工場叩き上げから起業までの話

- 切り口: スポーツトレーナー専門学校中退 → 町工場での整備・鈑金塗装・
  コーティング・用品取付経験 → 独立して47都道府県で出張作業 → 法人化という
  代表・堀越友輔のキャリアパス。
- 想定読者: 整備・鈑金・コーティング業界の経営者、起業に関心がある同業者。
- 状態: 未着手
- メモ: CLAUDE.md の代表者情報が一次ソース。具体的なエピソードは別途取材が必要
  （現時点では確度の高い詳細情報なし）。

## なぜ「証明書 × 改ざん検知」を作ったのか

- 切り口: 自動車業界の信頼低下・大手の品質不正を背景に、現場の整備士だった
  代表が「証拠を残せる仕組み」としてLedraを作った経緯。
- 想定読者: エンドユーザー（車両オーナー）、整備店オーナー。
- 状態: 未着手
- メモ: README.md の「施工証明書 × 改ざん検知」の思想（Polygonアンカリング +
  施工前後写真ゲート）と接続できる。

## 出張整備47都道府県でわかった現場の課題

- 切り口: 独立後、全国を出張作業で回った経験から見えた業界特有の課題
  （情報の非対称性、記録が残らないことによるトラブル等）。
- 想定読者: 業界関係者、一般消費者。
- 状態: 未着手
- メモ: `docs/internal/information-asymmetry-thesis.md` に関連する検討が
  既にある可能性。執筆前に参照すること。

## 「現場に記録作業を増やさない」ソフトの作り方

- 切り口: 職人が普段どおり作業するだけで、必要な記録・証明・共有が自然に残る——
  写真は撮るだけ・メモは話すだけ・不足は後からAIが検出、という現場負担ゼロ設計の思想。
  点検フォームを音声/カメラ直行/Storage写真に格上げした改善（＝機能を足すのではなく
  入力の手間を消す）を具体例に。
- 想定読者: 整備・鈑金・コーティング業界の経営者、現場DXに関心のある同業者。
- 状態: 未着手
- メモ: `docs/field-workflow-roadmap.md`（現場負担軽減ロードマップ）と
  `docs/feature-audit-brushup-2026-07.md` の提案1が一次ソース。
  機密になりうる機能構成の粒度は出さず、「思想」に抽象化して書く（公開区分: 公開可の範囲で）。

## 「使われる機能」だけ残す — SaaSの機能別利用率を運営が毎月見る話
- 切り口: 多機能SaaSほど「作ったが使われない機能」が溜まる。Ledraでは運営が
  店舗ごとの機能別利用率（予約/作業記録/請求/証明書/顧客/決済を当月使った店舗の割合）を
  毎月確認する仕組みを入れた。数字で「機能の生死」を判断する運用の話。
- 想定読者: SaaS/プロダクト開発者、同業の経営者。
- 状態: 未着手
- メモ: ログイン「回数」は追跡テーブルが無く実測できなかったため、last_sign_in_at
  ベースの「アクティブ会員」で近似した、という設計上の割り切り（捏造せず既存データで
  honest に近似する）もエピソードとして使える。数字・店舗名など内部指標は転記しない。

## 「動かない機能は、見せない」——ログイン画面を迷わせない設計にした話
- 切り口: 便利そうな選択肢（メールリンク/SSO）でも、環境が整わず確実に通らないなら入口から外す。UI に選択肢を
  増やすほどユーザーは迷う。honest な UX = できないことを見せない、という判断の実例。
- 想定読者: 小さく速く作る SaaS 開発者 / プロダクト志向の整備・車業界の経営者
- 状態: 未着手
- メモ: パスワードのみのログインに簡素化。バックエンドは温存して将来復活できる形にした点（撤去=破棄ではない）も材料。
  ※ SSO の具体的な想定導入先（顧客名）や Supabase 設定詳細は書かない。

## 「.single() は1件じゃないと落ちる」——招待した瞬間ログインできなくなった話
- 切り口: スタッフを招待したら本人がログインできない。原因はDB取得の .single() が「0件でも2件でも
  エラー」という仕様。招待された人が過去に自分でも登録していて所属が2件になった瞬間に露呈した。
  Web側は既に正しく直っていて、モバイルだけ取り残されていた＝「同じ処理を2箇所に書くと片方だけ古くなる」教訓。
- 想定読者: 小さく速く作る SaaS 開発者 / 車・整備業界で内製している人
- 状態: 未着手
- メモ: 修正は「最古の所属を1件だけ採る」で Web と挙動を統一。個人のメールアドレス・テナント名など内部情報は転記しない。

## 「モバイルで撮った写真は端末に残さない」——証拠写真をDBへ直行させる設計
- 切り口: 現場スマホの施工写真を、その場のカメラ撮影に限定（アルバム選択は不可）して、
  ハッシュ・撮影時刻・段階タグ付きでDBへ直行させる。端末のギャラリーには残さず、後から必要なときだけ
  明示ボタンで保存できる。「古い写真・加工写真を混ぜられない」＝証拠としての写真の作り方の話。
  おまけに、サーバ側の“正しい処理入口”は元々あったのにアプリから呼ばれておらず、しかも旧経路は
  存在しない保存先に書いていて動いていなかった——「1系統に寄せる」設計整理の教訓も材料。
- 想定読者: 車・整備/コーティング業界の経営者 / 証拠性・改ざん対策を気にする実務者 / 小さく作るSaaS開発者
- 状態: 未着手
- メモ: 具体的なプロジェクトID・スキーマ内部・匿名キー等の機密は書かない。真正性グレードの内部名や
  端末アテステーションの実装詳細も伏せ、「その場撮影・端末非保存・後からDL」という体験の骨子に絞る。

## 「カテゴリと検索を足しても羅列は直らなかった」——原因は並べ方と描画量だった話
- 切り口: 「品目一覧が無造作な羅列」と言われて、カテゴリ分けと検索を足した。次のテストでまた同じ指摘。
  原因は絞り込み機能の不足ではなく、(1) 品目名の長さで幅が決まるボタンを折り返し配置していたこと、
  (2) 全品目を一度に描画していたこと、の2つだった。周辺を足しても、原因に触れなければ症状は残る。
  等幅タイルのグリッドと画面分だけ描く仕組みに変えたら、同じ機能のまま印象が変わった。
- 想定読者: 小さく速く作る SaaS 開発者 / 現場で使う業務アプリを内製している人
- 状態: 未着手
- メモ: 「言われた機能を足す」と「言われた不満の原因を直す」は別物、という一般論に落とせる。
  具体的な品目名・価格・店舗名は書かない。

## 「タブが5枚だと、+ ボタンは真ん中に置けない」——UIの選択肢を決めるのは幾何の制約
- 切り口: 「タブを丸くして、センターに+ボタン」と言われた。だがタブが5枚あると、+ を列の中に入れた瞬間
  6スロットになり、どこに置いても中心は 41.7% か 58.3% にしかならない。真ん中に置ける形は
  「タブを4枚に減らす」か「列の上に浮かせる」の2つだけ。今回は5枚目のタブが15本の導線を持つハブだったので、
  減らさずに浮かせた。デザインの要望が物理的に何を要求しているかを先に計算すると、選択肢が2つに絞れる。
- 想定読者: プロダクト志向の経営者 / デザインと実装の間で判断している人
- 状態: 未着手
- メモ: 「できません」ではなく「この形なら成立し、代償はこれ」と出す進め方の実例。アプリの内部構成の
  細部（画面名の羅列など）は書かない。

## 「自分が書いたコードにレビューをかけたら、実害4件が出てきた」
- 切り口: UI改修を書き上げて型チェックもテストも通った状態で、自動コードレビューを回した。出てきたのは
  見た目の指摘ではなく、(1) 自由入力の品目を追加する導線が特定条件で完全に消える、(2) タブレットの
  画面分割で決済手段の判定が取引中に入れ替わる、(3) 端末の戻る操作でカートが黙って消える、
  (4) セーフエリアの計算漏れでラベルがナビゲーションバーに潜る——全部、自分が直前に入れた変更が原因。
  「動いた」と「壊れていない」は別。
- 想定読者: 小さく速く作る SaaS 開発者 / AIにコードを書かせている実務者
- 状態: 未着手
- メモ: 金額まわり（請求額と記帳額のずれ）は特に効く材料。ただし決済事業者名や内部の関数名・
  テーブル名の詳細は書かない。

## 「3回直して3回とも直らなかったバグ」——修正が届いていない場所に書かれていた話
- 切り口: 「戻るボタンが出ない」と3回言われた。1回目も2回目も、正しい形の修正コードを書いていた。
  問題は書いた場所で、同じ画面のファイルが2つあり、自分が直した方は表示されない側だった。
  症状（ボタンが無い）に対して正しい対策（ボタンを足す）を打っても、対象が違えば0点。
  3回目でようやく「なぜ効かないのか」を先に調べ、重複そのものを消して解決した。
  同じコードを8箇所に手書きしていた構造も、書き漏らしを生む温床として共有化した。
- 想定読者: 小さく速く作る SaaS 開発者 / AIにコードを書かせている実務者 / 現場で内製している人
- 状態: 未着手
- メモ: 「直したのに直らない」ときは、修正内容ではなく修正が届いているかを疑う、という教訓に落とせる。
  ついでに、押しても何も起きないメニュー項目が6つ見つかった件も材料になる（できないことを見せない）。

## 「判定ロジックはあるのに、誰も呼んでいなかった」——発行ゲートを4つの経路に配線し直した話
- 切り口: 「証明書の発行は◯◯条件を全部満たさないとダメ」というルールを判定する関数は、テスト付きで
  ちゃんと実装されていた。ところが実際に証明書を「発行済み」にする4つの入口（管理画面・アプリ・
  オフライン経路・AI自動発行）は、どれもその関数を一度も呼んでいなかった——写真の有無だけを
  それぞれが個別に手書きでチェックしていた。「型やロジックを作った」と「実際に使われている」は別物。
  配線し直す過程で、別の判定（署名依頼）が「証明書が発行済みであること」を前提にしている設計と
  正面衝突することも判明した。発行→署名→発行という無限ループになりかねない箇所で、配線する前に
  気づけたので事なきを得た。
- 想定読者: 小さく速く作る SaaS 開発者 / AIにコードを書かせている実務者 / 業務システムを内製している人
- 状態: 未着手
- メモ: 「ロジックが存在する」と「呼ばれている」の間には検証しないと埋まらないギャップがある、という
  一般論に落とせる。加えて、AIによるコードレビューで「4つの入口のうち2つでは、条件の一部が
  実は素通りしていた」という見落としも見つかった（懸念の紐付け方が経路によって違っていた）——
  自分で書いたロジックを自分でレビューしても見えない穴がある例として使える。具体的なテーブル名・
  関数名・社名は書かない。
